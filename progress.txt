# 3D Earth Visualization - Implementation Progress

## Completed Tasks

### BUG-001: Remove duplicate getDayOfYear function
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Commit:** 3887624
- **Changes:** Removed duplicate `getDayOfYear` function from index.html. The function was defined twice (originally at lines 190 and 445) with identical implementation. Kept the first definition and removed the duplicate.

### BUG-002: Add Subresource Integrity (SRI) for CDN scripts
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added SRI `integrity` and `crossorigin="anonymous"` attributes to both CDN script tags:
  - Three.js r128: `sha384-CI3ELBVUz9XQO+97x6nwMDPosPR5XvsxW2ua7N1Xeygeh1IxtgqtCkGfQY9WWdHu`
  - OrbitControls: `sha384-wagZhIFgY4hD+7awjQjR4e2E294y6J2HSnd8eTNc15ZubTeQeVRZwhQJ+W6hnBsf`

### BUG-003: Fix potential XSS vulnerability in showError function
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Replaced unsafe `innerHTML` usage with safe DOM manipulation:
  - Refactored `showError()` function to use `document.createElement()` and `textContent` instead of template literals with `innerHTML`
  - Updated `initApp()` error handler to use safe DOM methods instead of `innerHTML +=`
  - Updated error messages to use plain text (removed `<br>` tags that relied on HTML parsing)
  - Malicious input is now escaped automatically since `textContent` treats all input as text

### BUG-004: Remove unused texture uniforms from shader
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Removed unused `normalTexture` and `specularTexture` that were loaded but never used in the fragment shader:
  - Removed texture loading for `textures/earth_normal.jpg` and `textures/earth_specular.jpg`
  - Removed `normalTexture` and `specularTexture` uniforms from ShaderMaterial
  - Removed unused uniform declarations from fragment shader
  - This reduces bandwidth usage and eliminates unnecessary texture downloads

### BUG-005: Cache DOM element references for performance
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Cached the 'currentTime' DOM element reference to avoid repeated getElementById calls:
  - Added `timeDisplayElement` global variable to store the cached reference
  - Modified `initTimeControls()` to cache the element reference during initialization
  - Updated `updateTimeDisplay()` to use the cached reference instead of calling `document.getElementById('currentTime')` on every update
  - This eliminates unnecessary DOM queries during slider movement for improved performance

### REFACTOR-001: Migrate to Astro + TypeScript project structure
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Converted single-file application to modular Astro project with TypeScript:
  - Created package.json with pnpm, Astro 5.16, Three.js 0.128, TypeScript 5.9, and @astrojs/cloudflare adapter
  - Created tsconfig.json with strict mode enabled and all strict checks
  - Created astro.config.mjs with Cloudflare adapter for static output
  - Created wrangler.toml with [assets] directory = "./dist" (per project guidelines)
  - Migrated HTML to src/pages/index.astro
  - Extracted CSS to src/styles/global.css
  - Moved textures to public/textures/
  - Created modular TypeScript architecture in src/scripts/earth/:
    - types.ts: All TypeScript interfaces (EarthConfig, SceneObjects, EarthObjects, etc.)
    - config.ts: Configuration constants with typed interface
    - scene.ts: Scene initialization, WebGL support checking, error display
    - earth.ts: Earth mesh, shader materials, cloud layer
    - lighting.ts: Ambient and directional lighting
    - astronomy.ts: NOAA sun position calculations, day of year
    - controls.ts: OrbitControls initialization
    - geolocation.ts: Browser geolocation handling
    - ui.ts: Time controls, event listeners, display updates
    - index.ts: Module re-exports
  - Created src/scripts/main.ts as orchestrating entry point
  - Build completes without TypeScript errors
  - No 'any' types used
  - All imports use relative paths (../scripts/)
  - Removed old index.html

### FEATURE-004: Add interactive location markers for major cities
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented interactive city markers with tooltips showing local time:
  - Created new `src/scripts/earth/markers.ts` module with full TypeScript types
  - Added 12 major world cities: London, New York, Tokyo, Sydney, Dubai, São Paulo, Mumbai, Beijing, Paris, Los Angeles, Moscow, Cairo
  - Each city has coordinates (lat/lon) and timezone offset for accurate local time calculation
  - Markers use THREE.Sprite with canvas-generated textures for billboard rendering (always face camera)
  - City markers are yellow (#ffcc00) filled circles with white borders
  - User location marker is green (#00ff88) with ring design for distinction
  - Implemented `latLonToPosition()` function for spherical coordinate conversion
  - Tooltips show city name and calculated local time based on time slider position
  - Local time calculation accounts for user's local timezone offset
  - Added UI toggle checkbox "Show city markers" to show/hide all markers
  - Markers rotate with Earth mesh to stay at correct geographic positions
  - Added `CityData` and `MarkerState` interfaces to types.ts
  - Updated index.ts exports and main.ts integration
  - All TypeScript strict mode checks pass
  - Build completes without errors

### FEATURE-002: Implement Earth's axial tilt for accurate seasonal visualization
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added Earth's 23.4 degree axial tilt for realistic seasonal day/night patterns:
  - Added `AXIAL_TILT: 23.4` constant to config.ts
  - Added `AXIAL_TILT` property to EarthConfig interface in types.ts
  - Modified `initEarth()` in earth.ts to apply the tilt to both Earth mesh and clouds layer via `rotation.z`
  - Updated `updateEarthRotation()` to preserve the tilt when adjusting longitude-based rotation
  - The tilt affects how the day/night terminator appears at different times of year
  - Arctic region now correctly shows near-24h daylight on June 21 (summer solstice)
  - Arctic region now correctly shows near-24h darkness on December 21 (winter solstice)
  - Equinoxes (March 20, September 22) show terminator passing through both poles
  - Build completes without TypeScript errors

### FEATURE-003: Add date picker for seasonal exploration
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added date picker UI and connected it to sun position calculations:
  - Date picker UI element already existed in index.astro
  - TimeState interface already included `datePicker`, `lastDateValue`, and `selectedDate` fields
  - Helper functions `formatDateForInput()` and `parseDateFromInput()` already in ui.ts
  - Date picker initialization with current date as default already implemented
  - Event listener for date changes already added, updating `selectedDate` in TimeState
  - **Fixed bug:** `handleSunUpdate()` now passes `timeState.selectedDate` to `updateSunPosition()`
  - **Fixed bug:** `handleSunUpdate()` now detects date changes via `lastDateValue` comparison
  - Sun position now correctly updates when date is changed
  - Solar declination changes based on selected date, affecting day/night patterns
  - Time slider continues to work correctly in combination with date picker
  - Selecting June 21 shows longer daylight in Northern Hemisphere
  - Selecting December 21 shows shorter daylight in Northern Hemisphere
  - Build completes without TypeScript errors

### FEATURE-001: Add stars background for immersive space environment
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented a star field using THREE.Points for efficient rendering:
  - Created new `src/scripts/earth/stars.ts` module with full TypeScript types
  - Added `StarsConfig` interface for configurable star parameters
  - Stars use 5000 points distributed uniformly on a sphere with radius 400 (well behind Earth)
  - Used uniform spherical distribution algorithm for even star coverage in all directions
  - Stars rendered with THREE.PointsMaterial (white, size 1.5, slight transparency)
  - Stars positioned at correct z-depth (always behind Earth and clouds due to large radius)
  - Added to scene immediately after scene initialization but before Earth
  - Updated `src/scripts/earth/index.ts` to export stars module
  - Updated `src/scripts/main.ts` to initialize stars after scene setup
  - Build completes without TypeScript errors
  - No performance impact (particle systems are highly efficient)

### FEATURE-005: Add toggle for Earth axis and reference lines visualization
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented reference lines showing Earth's axis and latitude circles:
  - Created new `src/scripts/earth/reflines.ts` module with full TypeScript types
  - Added `ReferenceLinesState` interface to types.ts
  - Axis line (blue, 0x00aaff) extends through North and South poles beyond Earth surface
  - Equator line (orange, 0xffaa00) rendered as circle at 0 degrees latitude
  - Arctic Circle (green, 0x88ff88) rendered at 66.5 degrees N latitude
  - Antarctic Circle (green, 0x88ff88) rendered at 66.5 degrees S latitude
  - All lines use semi-transparent material (opacity 0.4) to avoid obscuring Earth texture
  - Reference lines grouped in THREE.Group with Earth's axial tilt applied
  - Lines rotate with Earth mesh to maintain correct geographic positions
  - Added UI toggle checkbox "Show reference lines" - disabled by default
  - Updated index.ts exports and main.ts integration
  - All TypeScript strict mode checks pass

### IMPROVEMENT-001: Add loading indicator for texture downloads
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented a comprehensive loading system with progress tracking and error recovery:
  - Created loading overlay UI in index.astro with spinner animation and progress bar
  - Added CSS styles for loading indicator with fade-out animation and error state
  - Created new `src/scripts/earth/loading.ts` module with full TypeScript types:
    - `LoadingState` interface for managing loading UI state
    - `createLoadingState()` to initialize loading state from DOM elements
    - `createTextureLoadingManager()` to create THREE.LoadingManager with progress tracking
    - `updateLoadingProgress()` for real-time progress updates
    - `hideLoadingOverlay()` with smooth fade-out animation
    - `showErrorOverlay()` and `hideErrorOverlay()` for error handling
    - `setupRetryButton()` for error recovery functionality
  - Modified `initEarth()` in earth.ts to accept LoadingState and use the loading manager
  - Updated main.ts to initialize loading state and wire up retry functionality
  - Loading overlay shows percentage progress during texture downloads
  - Error overlay displays when texture loading fails with retry button
  - Canvas remains hidden until all textures are loaded (no flash of incomplete content)
  - Build completes without TypeScript errors

### IMPROVEMENT-002: Add unit tests with Vitest
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented comprehensive unit test suite using Vitest:
  - Added `vitest` and `@vitest/coverage-v8` to devDependencies
  - Created `vitest.config.ts` with test configuration and coverage thresholds
  - Added test scripts: `pnpm test`, `pnpm test:watch`, `pnpm test:coverage`
  - Created test files:
    - `src/scripts/earth/astronomy.test.ts`: 17 tests for getDayOfYear() and calculateSunPosition()
      - Edge cases: Jan 1, Dec 31, leap year handling
      - Sun position calculations with known astronomical data
      - Summer/winter solstice declination verification
      - Equinox declination near-zero verification
    - `src/scripts/earth/markers.test.ts`: 23 tests for latLonToPosition() and calculateLocalTime()
      - Coordinate conversion for poles, equator, cities
      - Position magnitude verification
      - Local time calculation with various timezone offsets
      - Day rollover handling, fractional timezones (India UTC+5:30)
      - CITIES constant validation
    - `src/scripts/earth/config.test.ts`: 20 tests for CONFIG constant validation
      - Physical properties, time constants, camera settings
      - Lighting, visual effects, default location, controls
  - All 60 tests pass
  - Tests run in under 500ms
  - Build continues to work without errors

### FEATURE-011: Add time scrubber for full day visualization
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Enhanced time controls with play/pause functionality and speed controls:
  - Updated `TimeState` interface in types.ts with playback properties:
    - `isPlaying`: boolean to track playback state
    - `playbackSpeed`: number (1, 10, 60, 360 multipliers)
    - `lastPlaybackTime`: timestamp for calculating elapsed time
  - Added playback UI in index.astro:
    - Play/pause button with ▶ and ⏸ icons
    - Speed selector dropdown with 1×, 10×, 60× (default), and 360× options
  - Implemented playback logic in ui.ts:
    - `togglePlayback()`: toggle play/pause state and update UI
    - `setPlaybackSpeed()`: change playback speed multiplier
    - `stepTime()`: step forward/backward by slider step amount
    - `updatePlayback()`: calculate and apply time progression during playback
    - `updatePlaybackUI()`: sync play/pause icons with state
  - Added keyboard shortcuts:
    - Space: Play/Pause
    - Left Arrow: Step time backward
    - Right Arrow: Step time forward
  - Updated main.ts animate loop to call `updatePlayback()` each frame
  - Added CSS styles for playback controls:
    - Circular play/pause button with hover/active states
    - Speed selector styled to match other controls
  - Time wraps around at midnight (1440 minutes)
  - All 60 existing tests pass
  - Build completes without TypeScript errors

### FEATURE-012: Add fly-to animation for city markers
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented smooth camera fly-to animation when clicking city markers:
  - Created new `src/scripts/earth/flyTo.ts` module with full TypeScript types:
    - `FlyToState` interface for managing animation state
    - `createFlyToState()` to initialize animation state
    - `startFlyTo()` to begin camera animation to a city
    - `updateFlyTo()` to update animation each frame
    - `cancelFlyTo()` to interrupt animation on user interaction
    - `isFlyingTo()` helper to check animation status
  - Camera follows curved path using spherical interpolation (not straight line)
  - Animation duration 2-3 seconds based on angular distance
  - Ease-in-out cubic easing for smooth motion
  - Added click handler for city markers in main.ts
  - Added `handleMarkerClick()` function in markers.ts
  - User can interrupt animation via mouse drag or scroll wheel
  - Clicking another marker during animation starts new fly-to
  - Added tooltip hint "Click to fly to" on marker hover
  - Added pointer cursor style when hovering over markers
  - Controls disabled during animation, re-enabled on completion
  - Added 21 unit tests for fly-to functionality
  - All 81 tests pass
  - Build completes without TypeScript errors

### FEATURE-007: Add atmospheric glow effect at Earth's edge
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented atmospheric glow using Fresnel shader for realistic limb effect:
  - Created new `src/scripts/earth/atmosphere.ts` module with full TypeScript types:
    - `AtmosphereState` interface for managing atmosphere mesh and visibility
    - `initAtmosphere()` creates sphere with Fresnel shader material
    - `updateAtmosphereRotation()` syncs atmosphere with Earth rotation
    - `setAtmosphereVisible()` toggles visibility
  - Fresnel shader creates blue glow at Earth's edge (limb effect):
    - Vertex shader computes world position for view direction calculation
    - Fragment shader uses Fresnel equation: stronger glow at edges
    - Uses `THREE.BackSide` rendering for glow effect
    - Uses `THREE.AdditiveBlending` for light accumulation
  - Added atmosphere configuration constants to config.ts:
    - `ATMOSPHERE_RADIUS: 5.15` (slightly larger than clouds)
    - `ATMOSPHERE_COLOR: 0x88ccff` (sky blue)
    - `ATMOSPHERE_GLOW_INTENSITY: 0.6` (subtle glow)
    - `ATMOSPHERE_FRESNEL_POWER: 3.0` (controls edge sharpness)
  - Added `AtmosphereState` interface to types.ts
  - Added UI toggle checkbox "Show atmosphere" (enabled by default)
  - Atmosphere rotates with Earth mesh
  - Updated index.ts exports and main.ts integration
  - Added 4 unit tests for atmosphere config validation
  - All 85 tests pass
  - Build completes without TypeScript errors

### FEATURE-013: Expand city markers to 50+ locations with search
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Expanded city markers from 12 to 55 cities and added search functionality:
  - Expanded CITIES array to 55 cities spanning all continents:
    - Europe (14): London, Paris, Berlin, Madrid, Rome, Amsterdam, Stockholm, Athens, Istanbul, Moscow, Warsaw, Lisbon, Dublin, Helsinki
    - North America (9): New York, Los Angeles, Chicago, Toronto, Mexico City, Vancouver, Miami, San Francisco, Houston
    - South America (6): São Paulo, Buenos Aires, Rio de Janeiro, Lima, Bogotá, Santiago
    - Asia (15): Tokyo, Beijing, Shanghai, Mumbai, Delhi, Dubai, Singapore, Hong Kong, Seoul, Bangkok, Jakarta, Taipei, Riyadh, Tehran, Karachi
    - Africa (7): Cairo, Lagos, Nairobi, Johannesburg, Casablanca, Cape Town, Addis Ababa
    - Oceania (4): Sydney, Melbourne, Auckland, Perth
  - Added city search input with autocomplete dropdown in controls panel
  - Search is case-insensitive and supports partial matching
  - Search results debounced at 200ms for performance
  - Selecting a search result triggers fly-to animation to the city
  - Clear button to reset search input
  - Results limited to 8 matches in dropdown
  - Added `CitySearchState` interface to types.ts
  - Added `searchCities()`, `initCitySearch()`, `updateSearchResults()`, `clearCitySearch()` functions to markers.ts
  - Added CSS styles for search input, results dropdown, and clear button
  - Added 6 unit tests for `searchCities()` function
  - All 91 tests pass
  - TypeScript compiles without errors

### FEATURE-016: Add solstice and equinox preset buttons
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Added quick-access preset buttons for astronomically significant dates:
  - Added four preset buttons below the date picker: Spring Eq, Summer Sol, Autumn Eq, Winter Sol
  - Spring Equinox sets date to March 20 of current year
  - Summer Solstice sets date to June 21 of current year
  - Autumn Equinox sets date to September 22 of current year
  - Winter Solstice sets date to December 21 of current year
  - Clicking a preset sets the date and time to noon UTC for optimal visualization
  - Active preset button is visually highlighted with blue border and background
  - Active state clears when user manually changes the date picker
  - Each button has a tooltip explaining the astronomical significance
  - Added `getAstronomicalPresets()` exported function for testability
  - Buttons styled consistently with existing UI controls
  - All 91 existing tests pass
  - Build completes without TypeScript errors

### FEATURE-017: Highlight the day/night terminator line
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented visible terminator line at the day/night boundary with glow effect:
  - Created new `src/scripts/earth/terminator.ts` module with full TypeScript types:
    - `generateTerminatorPoints()` computes great circle points perpendicular to sun direction
    - `initTerminator()` creates the line geometry and glow line
    - `updateTerminatorPosition()` updates geometry each frame based on sun direction
    - `setTerminatorVisible()` toggles visibility
  - Terminator is the great circle where `dot(surfaceNormal, sunDirection) = 0`
  - Algorithm constructs orthonormal basis (U, V) in the plane perpendicular to sun direction
  - Points generated as `R * (cos(t) * U + sin(t) * V)` for 128 segments
  - Main line uses orange/gold color (0xffaa44) at 0.7 opacity
  - Glow line uses deeper orange (0xff8822) at 0.25 opacity behind the main line
  - Added `TerminatorState` interface to types.ts
  - Added UI toggle checkbox "Show terminator line" (disabled by default)
  - Terminator rotates with Earth mesh (same coordinate space as shader)
  - Earth's axial tilt applied to terminator group
  - Line updates in real-time as time/date changes
  - Updated index.ts exports and main.ts integration
  - All 91 tests pass
  - Build completes without TypeScript errors

### FEATURE-006: Add Moon visualization with correct orbital position and phases
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented Moon with orbital position calculation and phase lighting:
  - Created new `src/scripts/earth/moon.ts` module with full TypeScript types:
    - `calculateMoonPhaseAngle()` computes Moon phase using synodic period (29.53 days)
    - `calculateMoonPosition()` positions Moon relative to Sun-Earth line
    - `initMoon()` creates Moon mesh with custom shader for phase illumination
    - `updateMoonPosition()` updates position and lighting each frame
    - `setMoonVisible()` toggles visibility
  - Moon uses Lambertian lighting shader illuminated by sun direction for accurate phases:
    - New moon: Moon between Earth and Sun (dark face toward Earth)
    - Full moon: Earth between Moon and Sun (lit face toward Earth)
  - Orbital mechanics:
    - Phase angle calculated from reference new moon (Jan 6, 2000)
    - Orbital inclination of 5.14 degrees applied
    - Moon distance = 12x Earth radius (compressed from real 60x for visibility)
    - Moon radius = 0.2724x Earth radius (accurate ratio)
  - Tidal locking implemented: Moon always faces Earth (lookAt origin)
  - Added `MoonState` interface to types.ts
  - Added UI toggle checkbox "Show Moon" (disabled by default)
  - Moon position updates in real-time as time/date changes
  - Updated index.ts exports and main.ts integration
  - Added 10 unit tests for moon phase and position calculations
  - All 101 tests pass
  - TypeScript compiles without errors

### FEATURE-015: Display sunrise and sunset times for locations
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented sunrise/sunset calculation and display for all locations:
  - Created new `src/scripts/earth/sunrise.ts` module with full TypeScript types:
    - `SunTimes` interface for sunrise/sunset data with polar day/night flags
    - `calculateSunriseSunset()` uses NOAA algorithm with solar hour angle calculation
    - `formatSunTimesForTooltip()` formats sun times for marker tooltips
    - `updateSunTimesDisplay()` renders sunrise/sunset panel in UI
  - Algorithm details:
    - Uses NOAA equation of time and solar declination (same as astronomy.ts)
    - Calculates solar hour angle at sunrise/sunset using zenith angle 90.8333°
    - Accounts for atmospheric refraction and solar disk radius
    - Detects polar day (cosHourAngle < -1) and polar night (cosHourAngle > 1)
    - Converts UTC sunrise/sunset to local timezone
  - Updated marker tooltip in markers.ts to show sunrise/sunset times
    - `handleMarkerHover()` now accepts `selectedDate` parameter
    - Tooltip shows "Sun: ↑HH:MM ↓HH:MM" or polar day/night message
  - Added sunrise/sunset info panel in controls sidebar (index.astro)
    - Shows user location sunrise/sunset times
    - Shows selected city sunrise/sunset when clicking a city marker
    - Automatically updates when date changes
    - Styled with sunrise (gold) and sunset (orange) colors
  - Added CSS styles for sun times display in global.css
  - Updated main.ts integration:
    - Passes selectedDate to marker hover handler
    - Tracks selected city for sun times panel
    - Refreshes panel when date changes or city is clicked
  - Added 14 unit tests for sunrise calculations:
    - Normal mid-latitude locations
    - Sunrise before sunset verification
    - Arctic polar day (Tromsø, June 21)
    - Arctic polar night (Tromsø, December 21)
    - Antarctic polar day and night
    - Equator equinox accuracy (~6:00 sunrise, ~18:00 sunset)
    - Timezone offset handling (Tokyo +9)
    - Fractional timezone (India +5:30)
    - Summer vs winter day length comparison
    - HH:MM format validation
    - Tooltip formatting for all cases
  - All 115 tests pass
  - TypeScript compiles without errors

### FEATURE-009: Add satellite orbit visualization with ISS tracking
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented satellite orbit visualization with ISS, GPS, and Geostationary orbits:
  - Created new `src/scripts/earth/satellites.ts` module with full TypeScript types:
    - `SATELLITES` array with ISS (408km, 51.6° inclination, 92.68min period), GPS (20,200km, 55°), and Geostationary (35,786km, 0°) orbit data
    - `calculateSatellitePosition()` computes satellite position using simplified circular orbit model with RAAN precession
    - `generateOrbitPoints()` creates orbital track line geometry
    - `initSatellites()` creates orbit lines and marker sprites for all satellites
    - `updateSatellitePositions()` updates marker positions based on time and day of year
    - `updateSatellitesRotation()` keeps orbits synced with Earth rotation
    - `setSatellitesVisible()` toggles visibility
  - Orbit visualization:
    - ISS orbital path (red) at ~408km altitude with 51.6° inclination
    - GPS orbital path (green) at ~20,200km altitude with 55° inclination
    - Geostationary ring (blue) at ~35,786km in equatorial plane
    - Semi-transparent orbit track lines (opacity 0.35) for unobtrusive display
    - Satellite markers use canvas-generated sprites with glow effect
    - Marker scale varies by altitude for visibility
  - Orbital mechanics:
    - Position calculated from orbital period, inclination, and RAAN precession
    - ISS completes orbit every ~92.68 minutes
    - RAAN slowly precesses based on day of year
    - Geostationary orbit correctly stays in equatorial plane
  - Earth's axial tilt applied to satellite group
  - Added `SatelliteData` and `SatelliteState` interfaces to types.ts
  - Added UI toggle checkbox "Show satellite orbits" (disabled by default)
  - Updated index.ts exports and main.ts integration
  - Added 18 unit tests for satellite calculations:
    - SATELLITES constant validation
    - Orbital radius verification
    - Position periodicity (same position after one period)
    - Geostationary equatorial plane check
    - Orbit point generation and closed loop verification
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-003: Optimize mobile touch gesture handling
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented comprehensive mobile touch support for all interactions:
  - Updated `controls.ts`:
    - Disabled panning (not useful for globe) via `controls.enablePan = false`
    - Added `rotateSpeed: 0.8` for smoother touch rotation
    - Added `setupCanvasTouchHandling()` to prevent default browser touch behaviors on canvas
  - Updated `markers.ts`:
    - Added `detectMarkerAtPosition()` for coordinate-based marker detection (shared by mouse and touch)
    - Added `showMarkerTooltip()` and `hideMarkerTooltip()` utility functions
    - Refactored `handleMarkerHover()` to use shared detection/tooltip functions
  - Updated `main.ts`:
    - Added touch event listeners (touchstart, touchend, touchmove)
    - Implemented tap detection with distance threshold (15px) and timeout (300ms)
    - Touch tap on marker shows tooltip and triggers fly-to animation
    - Tooltip auto-hides after 3 seconds on touch devices
    - Multi-touch (pinch) cancels fly-to animation
    - Called `setupCanvasTouchHandling()` during initialization
  - Updated `index.astro`:
    - Viewport meta tag updated with `maximum-scale=1.0, user-scalable=no` to prevent double-tap zoom
    - Instructions updated to mention pinch zoom and tap-to-fly
  - Updated `global.css`:
    - Added `touch-action: none` on canvas for proper touch handling
    - Added `@media (pointer: coarse)` styles for minimum 44x44px touch targets
    - Ensured all interactive elements (buttons, inputs, checkboxes, search items) meet accessibility size requirements
    - Added responsive layout for controls panel on small screens (max-width: 480px)
    - Hidden instructions panel on touch devices to save screen space
    - Added `-webkit-overflow-scrolling: touch` for smooth scrolling in controls panel
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-004: Add accessibility features
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented comprehensive accessibility features for keyboard, screen reader, and reduced motion support:
  - Updated `src/pages/index.astro`:
    - Added `role="alert"` and `aria-live="polite"` to loading overlay
    - Added `role="progressbar"` with `aria-valuenow/min/max` to progress bar
    - Added `role="alert"` and `aria-live="assertive"` to error overlay
    - Added `role="region"` and `aria-label` to controls panel
    - Added `aria-label` to all checkboxes (clouds, markers, reference lines, terminator, moon, satellites, atmosphere)
    - Added `aria-label` to date picker, time slider, play/pause button, speed select
    - Added `aria-valuetext` to time slider for screen reader time announcements
    - Added `aria-hidden="true"` to decorative elements (spinner, error icon, play/pause icons)
    - Added `role="search"` and `aria-label` to city search container
    - Added `aria-autocomplete="list"` and `aria-controls` to search input
    - Added `role="listbox"` to search results container
    - Added `role="group"` and `aria-label` to preset buttons container
    - Added `aria-label` to each preset button
    - Added `aria-live="polite"` to time display and sun times display
    - Added `role="complementary"` to instructions panel
  - Updated `src/styles/global.css`:
    - Added `:focus-visible` styles with `2px solid #4da6ff` outline for all interactive elements
    - Added `@media (prefers-reduced-motion: reduce)` to disable loading spinner animation, fade transitions, and button transitions
    - Fixed color contrast: `.sun-times-loading` changed from `#666` to `#999` for WCAG AA compliance
  - Updated `src/scripts/earth/scene.ts`:
    - Added `role="img"` and `aria-label` to canvas element describing the 3D visualization
  - Updated `src/scripts/earth/ui.ts`:
    - Play/pause button `aria-label` updates dynamically based on playback state
    - Time slider `aria-valuetext` updates with human-readable time on every display update
  - Updated `src/scripts/earth/markers.ts`:
    - Search result items have `role="option"` attribute
    - Active search result items have `aria-selected="true"` attribute
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-005: Add JSDoc documentation for all modules
- **Status:** COMPLETED
- **Date:** 2026-01-30
- **Changes:** Added comprehensive JSDoc/TSDoc documentation to all 21 source modules and configured TypeDoc:
  - Added `@module` header documentation to all modules: types.ts, config.ts, scene.ts, earth.ts, lighting.ts, astronomy.ts, controls.ts, geolocation.ts, ui.ts, markers.ts, stars.ts, reflines.ts, loading.ts, flyTo.ts, atmosphere.ts, terminator.ts, moon.ts, sunrise.ts, satellites.ts, main.ts, index.ts
  - Added `@param` and `@returns` tags to all exported functions across all modules
  - Added property descriptions to all interfaces in types.ts (19 interfaces)
  - Added property descriptions to SunTimes, StarsConfig, and SatelliteData interfaces
  - Added `@example` code snippets for complex functions (latLonToPosition, getDayOfYear, CONFIG)
  - Added `@throws` tags where applicable (checkWebGLSupport, initWebGL)
  - Installed TypeDoc as dev dependency
  - Created `typedoc.json` configuration with entry points, output directory, and sorting options
  - Added `pnpm docs` and `pnpm docs:watch` scripts to package.json
  - TypeDoc successfully generates HTML documentation to `./docs` directory
  - 100% of exported functions, interfaces, and constants are documented
  - All 153 tests pass
  - TypeScript compiles without errors

### PERF-001: Implement Level-of-Detail (LOD) for Earth mesh
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented THREE.LOD for Earth and clouds with 3 detail levels:
  - Updated `src/scripts/earth/earth.ts`:
    - Replaced single `THREE.Mesh` with `THREE.LOD` for both Earth and clouds
    - Three LOD levels: High (128 segments, <15 units), Medium (64 segments, 15-30 units), Low (32 segments, >30 units)
    - Shared shader material across all LOD levels (no extra GPU memory for materials)
    - Added `updateEarthLOD()` function to update LOD each frame based on camera distance
    - Added `getLODLevels()` helper using config constants
  - Updated `src/scripts/earth/types.ts`:
    - Changed `EarthObjects.earth` from `THREE.Mesh` to `THREE.LOD`
    - Changed `EarthObjects.clouds` from `THREE.Mesh` to `THREE.LOD`
    - Added LOD config properties to `EarthConfig` interface
  - Updated `src/scripts/earth/config.ts`:
    - Added `LOD_HIGH_SEGMENTS: 128` for close-up detail
    - Added `LOD_HIGH_DISTANCE: 0`, `LOD_MEDIUM_DISTANCE: 15`, `LOD_LOW_DISTANCE: 30` thresholds
  - Updated `src/scripts/main.ts`:
    - Imported `updateEarthLOD` and called it each frame before render
  - Added 4 unit tests for LOD config validation
  - All 153 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-008: Add Aurora Borealis and Australis visualization
- **Status:** COMPLETED
- **Date:** 2026-01-30
- **Changes:** Implemented animated aurora effects near polar regions with night-side rendering:
  - Created new `src/scripts/earth/aurora.ts` module with full TypeScript types:
    - `createAuroraBandGeometry()` generates ring geometry at specified latitude band
    - `initAurora()` creates northern and southern aurora meshes with animated shader
    - `updateAurora()` advances animation time and updates sun direction uniform
    - `updateAuroraRotation()` syncs aurora group with Earth rotation
    - `setAuroraVisible()` toggles visibility
  - Custom GLSL shader for aurora curtain effect:
    - Vertex shader passes UV coordinates, world position, and normals
    - Fragment shader creates layered sine wave curtain with pseudo-noise for organic feel
    - Color gradient transitions from green (base) through blue to purple (top)
    - Shimmer effect using high-frequency noise
    - Night-side culling: auroras only visible on dark side using sun direction dot product
    - Smooth twilight transition using smoothstep
  - Aurora Borealis positioned at ~67 degrees N geomagnetic latitude
  - Aurora Australis positioned at ~67 degrees S geomagnetic latitude
  - Band width of 8 degrees for realistic aurora oval extent
  - Uses THREE.AdditiveBlending for light accumulation (glow effect)
  - Additive blending with transparency (opacity 0.45) for unobtrusive display
  - Earth's axial tilt applied to aurora group
  - Added `AuroraState` interface to types.ts
  - Added aurora config constants to config.ts and EarthConfig interface:
    - `AURORA_LATITUDE: 67`, `AURORA_BAND_WIDTH: 8`
    - `AURORA_OPACITY: 0.45`, `AURORA_ANIMATION_SPEED: 0.8`
  - Added UI toggle checkbox "Show auroras" (disabled by default)
  - Updated index.ts exports and main.ts integration
  - Animation runs at 60fps using delta-time-based updates
  - Added 11 unit tests for aurora config and geometry:
    - Config validation (latitude range, band width, opacity, speed)
    - Geometry vertex count, index buffer, radius, latitude placement
    - UV coordinate range, normal unit length
  - All 164 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-010: Add animated or real-time cloud layer
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented animated cloud drift simulating trade winds effect:
  - Created new `src/scripts/earth/clouds.ts` module with full TypeScript types:
    - `CloudAnimationState` interface for tracking animation mode and rotation offset
    - `createCloudAnimationState()` creates initial state (static mode, zero offset)
    - `setCloudAnimationMode()` switches between 'static' and 'animated' modes
    - `updateCloudAnimation()` applies differential rotation each frame
  - Cloud drift mechanics:
    - Clouds rotate slightly slower than Earth surface (trade winds effect)
    - Drift speed configurable via `CONFIG.CLOUD_DRIFT_SPEED` (0.002 radians/second)
    - Rotation offset wraps at 2*PI to prevent unbounded growth
    - Static mode locks clouds to Earth rotation (zero offset)
  - Added `CLOUD_DRIFT_SPEED` to config.ts and `EarthConfig` interface in types.ts
  - Added UI controls in index.astro:
    - Cloud mode select dropdown with "Static" and "Animated" options
    - Animated mode selected by default
    - Styled consistently with existing speed-select controls
  - Added CSS for `.cloud-mode-controls` and `.cloud-mode-select` in global.css
  - Mobile touch target size (44px minimum) applied to cloud mode select
  - Delta time computed once per frame and shared by cloud animation and aurora
  - Updated index.ts exports and main.ts integration
  - Cloud animation state reset in retry handler
  - Added 11 unit tests for cloud animation:
    - State creation defaults
    - Mode switching and offset reset
    - CONFIG.CLOUD_DRIFT_SPEED validation
    - Static vs animated behavior
    - Offset accumulation and wrapping
  - All 175 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-014: Allow users to add custom location markers
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented custom location markers with click-to-place, edit, delete, and localStorage persistence:
  - Created new `src/scripts/earth/customMarkers.ts` module with full TypeScript types:
    - `estimateTimezone()` approximates UTC offset from longitude (1 hour per 15 degrees)
    - `loadCustomMarkers()` loads persisted markers from localStorage with validation
    - `saveCustomMarkers()` persists markers to localStorage
    - `initCustomMarkers()` restores saved markers as sprites on the globe
    - `setPlacingMode()` toggles click-to-place mode with crosshair cursor
    - `screenToLatLon()` raycasts screen coordinates to geographic lat/lon, accounting for Earth rotation and axial tilt
    - `addCustomMarker()` creates a new custom marker with diamond-shaped magenta sprite
    - `removeCustomMarker()` deletes a marker and disposes its Three.js resources
    - `editCustomMarkerLabel()` updates a marker's label
    - `getCustomMarkerId()` extracts custom marker ID from sprite userData
    - `updateCustomMarkersList()` renders the marker list UI with edit/delete/fly-to actions
  - Added `CustomMarkerData` and `CustomMarkerState` interfaces to types.ts
  - Custom markers are visually distinct: magenta diamond shape vs yellow circles for cities
  - Click-to-place mode: button toggles crosshair cursor, click on globe opens prompt for label
  - Markers placed at correct geographic position using inverse Earth rotation transform
  - Edit and delete functionality via buttons in the marker list panel
  - Clicking a marker name triggers fly-to animation
  - Maximum 20 custom markers enforced
  - Markers show local time and sunrise/sunset in tooltips (reuses existing tooltip system)
  - Custom markers persist across page reloads via localStorage
  - Works on both desktop (click) and mobile (tap) for placement
  - Added UI section in index.astro with "Add marker" button and marker list
  - Added CSS styles for custom marker UI (button, list, edit/delete controls)
  - Updated main.ts with placement handlers for click and touch events
  - Added 13 unit tests for estimateTimezone, loadCustomMarkers, and saveCustomMarkers
  - All 188 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-018: Add solar eclipse visualization
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented solar eclipse visualization with shadow projection and eclipse database:
  - Created new `src/scripts/earth/eclipse.ts` module with full TypeScript types:
    - `ECLIPSES` database with 16 solar eclipses from 2024-2030 (total, annular, partial)
    - `findEclipseForDate()` finds eclipse matching a date string
    - `getNextEclipse()` returns the next upcoming eclipse from a given date
    - `initEclipse()` creates shadow mesh with custom shader material
    - `updateEclipseForDate()` positions shadow and configures shader based on eclipse type
    - `updateEclipseRotation()` syncs shadow with Earth rotation
    - `setEclipseVisible()` toggles visibility
    - `updateEclipseNotification()` shows/hides eclipse info in UI
    - `populateEclipseButtons()` creates quick-jump buttons for upcoming eclipses
  - Custom GLSL shader for eclipse shadow rendering:
    - Renders umbra (dark core) and penumbra (gradient shadow) on Earth surface
    - Different shadow profiles for total (darkest), annular (ring of fire), and partial eclipses
    - Uses angular distance from eclipse center for smooth gradient falloff
    - Shadow mesh slightly above Earth surface to prevent z-fighting
  - Added `EclipseData` and `EclipseState` interfaces to types.ts
  - Added UI controls in index.astro:
    - Toggle checkbox "Show eclipses" (disabled by default)
    - Eclipse notification banner showing eclipse type and description when date matches
    - Quick-jump buttons for next 5 upcoming eclipses with date and type
  - Eclipse shadow rotates with Earth mesh (axial tilt applied)
  - Notification appears automatically when navigating to an eclipse date
  - Quick-jump buttons set date to eclipse date and time to noon UTC
  - Added CSS styles for eclipse notification (orange theme) and quick-jump buttons
  - Updated index.ts exports and main.ts integration
  - Eclipse state properly reset in retry handler
  - Added 14 unit tests for eclipse module:
    - Database validation (field completeness, date sorting, year range, types)
    - findEclipseForDate() with known dates, non-eclipse dates, and empty string
    - getNextEclipse() with dates before, on, between, and after all eclipses
  - All 202 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-007: Add Progressive Web App (PWA) support
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented PWA support with web app manifest, service worker, and offline caching:
  - Created `public/manifest.webmanifest` with app metadata:
    - Full name "3D Earth Visualization", short name "Earth"
    - Standalone display mode for app-like experience
    - Theme and background color matching app (#0a1628)
    - SVG icons for both standard and maskable purposes
    - Education category classification
  - Created SVG icons in `public/icons/`:
    - `icon.svg`: Earth-themed icon with blue globe, green continents, and atmospheric glow
    - `icon-maskable.svg`: Same design with safe-zone padding for adaptive icons
  - Created `public/service-worker.js` with intelligent caching strategy:
    - Cache-first for textures (large files, rarely change)
    - Cache-first for static assets (`_astro/*`, `icons/*`)
    - Network-first for HTML and other assets (ensures fresh content)
    - Old cache cleanup on service worker activation
    - Graceful offline fallback for all request types
    - `skipWaiting()` and `clients.claim()` for immediate activation
  - Created `src/scripts/register-sw.ts` for service worker registration:
    - Registers service worker on window load event
    - Feature-detects `serviceWorker` API before registration
    - Logs registration errors to console
  - Updated `src/pages/index.astro` head section:
    - Added `<meta name="description">` for app description
    - Added `<meta name="theme-color" content="#0a1628">` for browser chrome
    - Added `<link rel="manifest">` pointing to webmanifest
    - Added `<link rel="icon">` with SVG favicon
    - Added `<link rel="apple-touch-icon">` for iOS home screen
  - App works fully offline after first load (all critical assets cached)
  - Install prompt appears on supported browsers
  - All 202 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-019: Display timezone boundaries on globe
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented timezone boundary meridian lines with hover tooltip:
  - Created new `src/scripts/earth/timezones.ts` module with full TypeScript types:
    - `getTimezoneInfos()` generates timezone info for all 25 standard meridians (-12 to +12)
    - `initTimezones()` creates meridian lines at 15-degree intervals on the globe
    - `updateTimezonesRotation()` syncs timezone lines with Earth rotation each frame
    - `setTimezonesVisible()` toggles visibility
    - `handleTimezoneHover()` raycasts mouse position to show timezone info tooltip
  - Timezone boundary visualization:
    - 25 meridian lines at 15-degree longitude intervals (-180 to +180)
    - Lines run pole-to-pole with configurable segment count (64 segments)
    - UTC/prime meridian highlighted in red (0xff4444) with higher opacity
    - Standard timezone lines in cyan-teal (0x44aacc) at 0.35 opacity
    - Lines rendered slightly above Earth surface to prevent z-fighting
  - Hover tooltip shows:
    - Timezone label (e.g., "UTC+5")
    - Current local time in that timezone based on time slider position
    - Geographic coordinates (latitude/longitude) of hover point
  - Added `TimezoneState` interface to types.ts
  - Added timezone config constants to config.ts and EarthConfig interface:
    - `TIMEZONE_LINE_COLOR: 0x44aacc`, `TIMEZONE_LINE_OPACITY: 0.35`
    - `TIMEZONE_UTC_COLOR: 0xff4444`, `TIMEZONE_SEGMENTS: 64`
  - Added UI toggle checkbox "Show timezone boundaries" (disabled by default)
  - Added CSS for timezone tooltip with fixed positioning and monospace font
  - Earth's axial tilt applied to timezone group
  - Timezone lines rotate with Earth mesh each frame
  - Updated index.ts exports and main.ts integration
  - Tooltip hidden when timezone visualization is disabled
  - Added 13 unit tests for timezone module:
    - Config validation (colors, opacity, segments)
    - getTimezoneInfos() entry count, boundaries, labels, sequential offsets
  - All 215 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### PERF-003: Implement GPU instancing for markers and particles
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented GPU instancing and shared material optimizations across star field, city markers, and satellite markers:
  - Updated `src/scripts/earth/stars.ts`:
    - Replaced `THREE.BufferGeometry` with `THREE.InstancedBufferGeometry` for per-star attributes
    - Added per-instance `offset` attribute (3D position on sphere)
    - Added per-instance `starSize` attribute for varying star brightness (0.3x to 2.5x base size)
    - Added per-instance `starColor` attribute for subtle warm/cool color variation
    - Custom GLSL vertex shader positions stars from offset attribute and applies size attenuation
    - Custom GLSL fragment shader renders circular points with soft edges
    - All 5000+ stars rendered in a single draw call with GPU-native instancing
    - Exported `STARS_CONFIG` for testability with new `minSizeMultiplier` and `maxSizeMultiplier` fields
  - Updated `src/scripts/earth/markers.ts`:
    - Added shared texture/material cache for city markers (`sharedCityTexture`, `sharedCityMaterial`)
    - Added shared texture/material cache for user location marker (`sharedUserTexture`, `sharedUserMaterial`)
    - All 55+ city markers now share a single `SpriteMaterial` instead of creating individual materials
    - Reduces material/texture GPU memory from 55 to 2 (city + user)
    - Marker hover and click detection continue to work unchanged via raycasting
  - Updated `src/scripts/earth/satellites.ts`:
    - Added `satelliteTextureCache` Map keyed by color to avoid redundant canvas texture creation
    - Satellite markers with same color reuse cached textures
  - Added 9 unit tests in `stars.test.ts`:
    - Star count, radius, base size validation
    - Size multiplier range and bounds validation
    - Minimum and maximum star size checks
    - Size attenuation enabled check
  - All 224 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### PERF-002: Implement texture compression with KTX2/Basis
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented KTX2/Basis Universal texture compression with automatic JPEG fallback:
  - Converted all three Earth textures to KTX2 format with Basis ETC1S encoding:
    - `earth_day.jpg` (4.4MB) → `earth_day.ktx2` (1.9MB) — 57% reduction
    - `earth_night.jpg` (3.0MB) → `earth_night.ktx2` (979KB) — 67% reduction
    - `earth_clouds.jpg` (12MB) → `earth_clouds.ktx2` (3.6MB) — 70% reduction
    - Total: 19.4MB → 6.5MB — **67% file size reduction** (exceeds 50% target)
  - Created new `src/scripts/earth/textureCompression.ts` module with full TypeScript types:
    - `KTX2_TEXTURES` and `JPEG_TEXTURES` path constants for both formats
    - `supportsCompressedTextures()` detects GPU compressed texture format support
    - `createKTX2Loader()` configures KTX2Loader with Basis transcoder path and GPU detection
    - `loadTexturesWithCompression()` tries KTX2 first, falls back to JPEG automatically
    - `loadJPEGTextures()` loads standard JPEG textures as fallback
  - Copied Basis Universal transcoder files to `public/basis/`:
    - `basis_transcoder.js` (63KB) — JavaScript transcoder
    - `basis_transcoder.wasm` (440KB) — WebAssembly transcoder
  - Updated `src/scripts/earth/earth.ts`:
    - `initEarth()` now uses `loadTexturesWithCompression()` instead of direct TextureLoader
    - Removed manual TextureLoader instantiation for Earth textures
    - Both KTX2 and JPEG paths use the same LoadingManager for progress tracking
  - KTX2Loader uses Three.js 0.128 built-in KTX2Loader + BasisTextureLoader:
    - Web Worker-based transcoding for non-blocking decompression
    - Automatic GPU format selection (S3TC, ETC1, ETC2, ASTC, BPTC, PVRTC)
    - GPU-native decompression (no CPU decode after transcoding)
  - Updated `src/scripts/earth/index.ts` with textureCompression exports
  - Added 8 unit tests for texture compression configuration:
    - KTX2 and JPEG texture path validation
    - File extension verification (.ktx2 and .jpg)
    - Directory path consistency
    - Matching keys between KTX2 and JPEG texture maps
    - Base filename consistency between compressed and fallback versions
  - All 232 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-006: Add WebGPU support for modern browsers
- **Status:** COMPLETED
- **Date:** 2026-02-01
- **Changes:** Implemented WebGPU detection, renderer abstraction, and WebGL fallback:
  - Created new `src/scripts/earth/webgpu.ts` module with full TypeScript types:
    - `RendererInfo` interface describing active rendering backend (webgpu/webgl)
    - `AppRenderer` interface providing common renderer API for both backends
    - `detectWebGPUSupport()` checks `navigator.gpu` availability
    - `verifyWebGPUAdapter()` asynchronously confirms GPU adapter accessibility
    - `createRenderer()` factory creates appropriate renderer with automatic backend selection
    - `WGSL_SHADERS` object containing WGSL equivalents of all GLSL shaders (earth vertex/fragment, atmosphere vertex/fragment)
    - `getWGSLShader()` helper to retrieve WGSL shader source by name
  - Renderer abstraction layer:
    - `AppRenderer` wraps `THREE.WebGLRenderer` (or `THREE.WebGPURenderer` when available)
    - Common interface: `domElement`, `setSize()`, `setClearColor()`, `render()`, `rendererInfo`, `nativeRenderer`
    - `nativeRenderer` provides direct access to underlying Three.js renderer for API-specific operations
    - `rendererInfo` reports backend type, WebGPU availability, and human-readable description
  - Updated `src/scripts/earth/types.ts`:
    - `SceneObjects.renderer` changed from `THREE.WebGLRenderer` to `AppRenderer`
    - Import added for `AppRenderer` type from webgpu module
  - Updated `src/scripts/earth/scene.ts`:
    - `initScene()` now uses `createRenderer()` factory instead of direct `THREE.WebGLRenderer`
    - Logs active rendering backend on initialization
    - Import added for `createRenderer` from webgpu module
  - Updated `src/scripts/earth/textureCompression.ts`:
    - `supportsCompressedTextures()` accepts `AppRenderer` and uses `nativeRenderer.extensions`
    - `createKTX2Loader()` accepts `AppRenderer` and passes `nativeRenderer` for GPU format detection
    - `loadTexturesWithCompression()` accepts `AppRenderer`
  - Updated `src/scripts/earth/flyTo.test.ts`:
    - Mock SceneObjects updated to use `AppRenderer` interface
  - Updated `src/pages/index.astro`:
    - Added `rendererInfo` div in instructions panel for displaying active backend
  - Updated `src/styles/global.css`:
    - Added `.renderer-info` styles for subtle renderer backend indicator
  - Updated `src/scripts/main.ts`:
    - Populates renderer info UI element with active backend name and description tooltip
  - Updated `src/scripts/earth/index.ts`:
    - Added webgpu module to barrel exports
  - WGSL shader ports:
    - Earth vertex shader ported to WGSL (VertexInput/VertexOutput structs, uniform bindings)
    - Earth fragment shader ported to WGSL (textureSample, smoothstep, sun direction dot product)
    - Atmosphere vertex shader ported to WGSL (world position, normal matrix transforms)
    - Atmosphere fragment shader ported to WGSL (Fresnel calculation, glow color/intensity)
  - WebGPU detection and logging:
    - Detects `navigator.gpu` API availability
    - Asynchronously verifies GPU adapter accessibility
    - Checks for `THREE.WebGPURenderer` availability in Three.js build
    - Comprehensive console logging for debugging renderer selection
  - WebGL fallback:
    - Automatic fallback when WebGPU not available or Three.js version lacks WebGPURenderer
    - All existing functionality preserved with WebGL rendering
    - No visual differences between WebGPU and WebGL paths
  - Added 19 unit tests for WebGPU module:
    - `detectWebGPUSupport()` with/without navigator.gpu
    - `verifyWebGPUAdapter()` success, null adapter, and error cases
    - WGSL shader content validation (vertex/fragment, earth/atmosphere)
    - `getWGSLShader()` name lookup
    - `RendererInfo` type structure for all backend configurations
  - All 251 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

## Pending Tasks

### Phase 1 - Bug Fixes
- [x] BUG-001: Remove duplicate getDayOfYear function
- [x] BUG-002: Add Subresource Integrity (SRI) for CDN scripts
- [x] BUG-003: Fix potential XSS vulnerability in showError function
- [x] BUG-004: Remove unused texture uniforms from shader
- [x] BUG-005: Cache DOM element references for performance

### Phase 2 - Refactoring
- [x] REFACTOR-001: Migrate to Astro + TypeScript project structure
- [x] REFACTOR-002: Create modular code architecture (completed as part of REFACTOR-001)

### Phase 3 - Core Features
- [x] FEATURE-002: Implement Earth's axial tilt
- [x] FEATURE-003: Add date picker for seasonal exploration

### Phase 4 - Visual Enhancements
- [x] FEATURE-001: Add stars background
- [x] FEATURE-004: Add interactive location markers for major cities
- [x] FEATURE-005: Add toggle for Earth axis and reference lines

### Phase 5 - Critical Improvements
- [x] IMPROVEMENT-001: Add loading indicator for texture downloads
- [x] IMPROVEMENT-002: Add unit tests with Vitest

### Phase 6 - Interactivity Enhancements
- [x] FEATURE-011: Add time scrubber for full day visualization
- [x] FEATURE-012: Add fly-to animation for city markers
- [x] FEATURE-013: Expand city markers to 50+ locations with search
- [x] FEATURE-015: Display sunrise and sunset times for locations

### Phase 7 - Astronomical Features
- [x] FEATURE-006: Add Moon visualization with orbital position and phases
- [x] FEATURE-016: Add solstice and equinox preset buttons
- [x] FEATURE-017: Highlight the day/night terminator line

### Phase 9 - Advanced Features
- [x] FEATURE-009: Add satellite orbit visualization with ISS tracking

### Phase 10 - Accessibility & Documentation
- [x] IMPROVEMENT-003: Optimize mobile touch gesture handling
- [x] IMPROVEMENT-004: Add accessibility features

### Phase 10 - Accessibility & Documentation (continued)
- [x] IMPROVEMENT-005: Add JSDoc documentation for all modules

### Phase 8 - Visual Polish
- [x] FEATURE-008: Add Aurora Borealis and Australis visualization
- [x] FEATURE-010: Add animated or real-time cloud layer

### Phase 9 - Advanced Features
- [x] FEATURE-014: Allow users to add custom location markers

### Phase 9 - Advanced Features (continued)
- [x] FEATURE-018: Add solar eclipse visualization

### Phase 9 - Advanced Features (continued)
- [x] FEATURE-019: Display timezone boundaries on globe

### Phase 11 - Performance & Future-proofing
- [x] PERF-001: Implement Level-of-Detail (LOD) for Earth mesh
- [x] IMPROVEMENT-007: Add Progressive Web App (PWA) support
- [x] PERF-003: Implement GPU instancing for markers and particles
- [x] PERF-002: Implement texture compression with KTX2/Basis
- [x] IMPROVEMENT-006: Add WebGPU support for modern browsers

### BUG-006: Overlay layers hidden behind cloud layer when clouds visible
- **Status:** COMPLETED
- **Date:** 2026-02-03
- **Changes:** Fixed overlay layers (terminator, reference lines, timezone boundaries) being hidden behind clouds:
  - Updated cloud layer material in `earth.ts` to set `depthWrite: false`, preventing clouds from occluding overlay layers
  - Increased overlay radii from 5.02-5.03 to 5.06 (above cloud layer at 5.05) for consistent rendering:
    - `terminator.ts`: radius increased to `CONFIG.EARTH_RADIUS + 0.06`
    - `reflines.ts`: radius increased to `CONFIG.EARTH_RADIUS + 0.06`
    - `timezones.ts`: radius increased to `CONFIG.EARTH_RADIUS + 0.06`
  - Render order now correct: Earth (5.0) → Clouds (5.05, no depth write) → Overlays (5.06)
  - No z-fighting artifacts between overlays and clouds
  - All overlay elements visually clear when rendered over clouds
- All 251 tests pass
- Build completes without errors

### BUG-007: App does not start centered on user's current location
- **Status:** COMPLETED
- **Date:** 2026-02-03
- **Changes:** Implemented smooth fly-to animation to user's geolocation on startup:
  - Updated `geolocation.ts`:
    - Added `GeolocationSuccessCallback` type for optional success callback
    - Modified `getLocation()` to accept optional `onSuccess` callback parameter
    - Callback is invoked with lat/lon when geolocation succeeds
  - Updated `main.ts`:
    - Modified `getLocationWithMarkerUpdate()` to accept geolocation success callback
    - Added callback that triggers fly-to animation to user's location
    - Creates synthetic `CityData` for user's location with estimated timezone from longitude
    - User marker placed immediately at default location (no rendering delay)
    - Smooth animation begins after geolocation permission granted
  - Behavior:
    - App renders immediately with default view (Stockholm, no delay)
    - When geolocation succeeds, camera smoothly flies to user's location
    - User's location marker is visible and centered after animation
    - Animation uses existing fly-to system (2-3 second duration, ease-in-out)
    - Graceful fallback: if geolocation denied/unavailable, stays at default view
  - All acceptance criteria met:
    - Globe automatically centers on user's geolocation after permission granted ✓
    - Centering happens with smooth animation (not instant snap) ✓
    - User's location marker visible in initial view ✓
    - Appropriate zoom level (uses CONFIG.INITIAL_CAMERA_DISTANCE) ✓
    - Graceful fallback to default view if geolocation denied ✓
    - No delay in rendering while waiting for geolocation ✓
- All 251 tests pass
- Build completes without errors

### FEATURE-020: Sun intensity visualization with subsolar point indicator
- **Status:** COMPLETED
- **Date:** 2026-02-05
- **Changes:** Implemented sun intensity visualization with subsolar point indicator and heat gradient:
  - Created new `src/scripts/earth/solarIntensity.ts` module with full TypeScript types:
    - `generateGroundRingPoints()` generates circular footprint points around subsolar point using spherical geometry
    - `initSolarIntensity()` creates spike line, ground ring, and heat gradient mesh with custom shader
    - `updateSolarIntensityPosition()` updates all components based on sun direction
    - `updateSolarIntensityRotation()` syncs visualization with Earth rotation
    - `setSolarIntensityVisible()` toggles visibility
  - Subsolar Point Indicator components:
    - Vertical spike/line extending from Earth surface (configurable height via `SOLAR_INTENSITY_SPIKE_HEIGHT`)
    - Circular ring footprint on Earth surface at subsolar point (configurable radius via `SOLAR_INTENSITY_RING_RADIUS`)
    - Both rendered in yellow (`SOLAR_INTENSITY_SPIKE_COLOR: 0xffff00`)
  - Heat Gradient Overlay:
    - Custom GLSL vertex/fragment shaders for radial heat visualization
    - Color gradient: bright yellow at center → orange → red toward edges
    - Angular distance calculation from subsolar point using dot product
    - Smooth falloff toward terminator using `smoothstep`
    - Night side fully transparent (uses sun direction check)
    - Additive blending for non-obscuring overlay effect
    - Configurable opacity via `SOLAR_INTENSITY_GRADIENT_OPACITY`
  - Added `SolarIntensityState` interface to types.ts
  - Added solar intensity config constants to config.ts and `EarthConfig` interface:
    - `SOLAR_INTENSITY_SPIKE_HEIGHT: 0.6`
    - `SOLAR_INTENSITY_SPIKE_COLOR: 0xffff00`
    - `SOLAR_INTENSITY_RING_RADIUS: 0.15`
    - `SOLAR_INTENSITY_GRADIENT_OPACITY: 0.5`
  - Added UI toggle checkbox "Show sun intensity" in index.astro (disabled by default)
  - Earth's axial tilt applied to visualization group
  - Visualization rotates with Earth mesh each frame
  - Position updates in real-time when time/date changes
  - Updated index.ts exports and main.ts integration
  - State properly reset in retry handler
  - Added 11 unit tests for solar intensity module:
    - Config validation (spike height, color, ring radius, gradient opacity)
    - Ground ring point generation (count, radius, closed loop)
    - Ring centering around subsolar point
    - Different sun positions (north pole, arbitrary position)
    - Different ring radii
  - All 263 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### UI-REFACTOR-001: Install and configure Tailwind CSS 4
- **Status:** COMPLETED
- **Date:** 2026-02-05
- **Changes:** Installed Tailwind CSS 4 via Astro integration and configured custom theme colors:
  - Ran `pnpm astro add tailwind --yes` to install Tailwind CSS 4 packages:
    - `tailwindcss@^4.1.18`
    - `@tailwindcss/vite@^4.1.18`
  - Astro config automatically updated with `@tailwindcss/vite` plugin
  - Updated `src/styles/global.css` with Tailwind CSS 4 syntax:
    - Added `@import "tailwindcss"` at the top of the file
    - Added `@theme` block with custom CSS properties for design system:
      - Accent colors: `--color-accent: #4da6ff`, `--color-accent-hover: #3d8fdb`, `--color-accent-active: #2d7cc4`
      - Panel/background colors: `--color-panel`, `--color-panel-darker`, `--color-input-bg`, `--color-input-border`
      - Text colors: `--color-text-primary` through `--color-text-dimmer`
      - Special colors: `--color-sunrise`, `--color-sunset`, `--color-error`, `--color-eclipse`, `--color-custom-marker`, `--color-timezone-line`, `--color-utc-line`
  - Existing CSS preserved alongside Tailwind layers
  - Tailwind utility classes available in all Astro components (flex, grid, hidden, absolute, etc.)
  - Build output includes both Tailwind base/utilities and custom styles
  - All 263 existing tests pass (no regressions)
  - Build completes without errors
  - Visual appearance unchanged from original design

### UI-REFACTOR-002: Create reusable UI base components
- **Status:** COMPLETED
- **Date:** 2026-02-05
- **Changes:** Created foundational Astro components for common UI patterns:
  - Created `src/components/ui/` directory structure
  - Created `src/components/ui/Toggle.astro`:
    - Checkbox toggle with label component
    - Props: id, label, checked, ariaLabel, class
    - Touch-friendly sizing for mobile (min 44px)
    - Focus-visible styles for accessibility
    - Uses Tailwind CSS custom properties for theming
  - Created `src/components/ui/Button.astro`:
    - Button component with four variants: primary, secondary, preset, icon
    - Props: id, variant, title, ariaLabel, disabled, type, class
    - Primary: accent-colored CTA button
    - Secondary: subtle action button with border
    - Preset: compact buttons for preset selections
    - Icon: circular icon button (e.g., play/pause)
    - Touch-friendly sizing and reduced motion support
  - Created `src/components/ui/Panel.astro`:
    - Semi-transparent panel container component
    - Props: id, variant, role, ariaLabel, class
    - Variants: default, darker, controls, instructions
    - Controls variant: fixed sidebar with scroll support
    - Instructions variant: fixed bottom-right helper panel
    - Backdrop blur effect for glass morphism
    - Mobile responsive adjustments
  - Created `src/components/ui/SearchInput.astro`:
    - Search input with clear button and dropdown slot
    - Props: id, placeholder, resultsId, clearButtonId, ariaLabel, class
    - Named slot "results" for dropdown content
    - Clear button hidden by default (shown via JS)
    - ARIA attributes for autocomplete accessibility
    - Touch-friendly sizing for mobile
  - Created `src/components/ui/index.ts` with component documentation
  - All components:
    - Use Tailwind CSS custom properties (--color-accent, --color-panel, etc.)
    - Preserve existing element IDs for JS compatibility
    - Include accessibility features (aria-label, focus-visible)
    - Support reduced motion preference
    - Have consistent styling patterns
  - All 263 tests pass
  - Build completes without errors
  - TypeScript compiles without errors

### UI-REFACTOR-003: Extract Time Controls section
- **Status:** COMPLETED
- **Date:** 2026-02-05
- **Changes:** Created TimeControls component grouping date picker, season presets, time slider, and playback controls:
  - Created `src/components/controls/TimeControls.astro`:
    - Date picker with season preset buttons (Spring Eq, Summer Sol, Autumn Eq, Winter Sol)
    - Time slider with current time display showing local time and UTC
    - Playback controls with play/pause button and speed selector (1x, 10x, 60x, 360x)
  - Two grouped sections with subtle dark background for visual hierarchy:
    - Date section: date picker input + season preset buttons
    - Time section: time slider + playback controls
  - All element IDs preserved for JS compatibility:
    - `datePicker`, `presetSpringEquinox`, `presetSummerSolstice`, `presetAutumnEquinox`, `presetWinterSolstice`
    - `timeSlider`, `currentTime`, `playPauseBtn`, `playIcon`, `pauseIcon`, `speedSelect`
  - Accessibility features:
    - ARIA labels on all interactive elements
    - `aria-live="polite"` on time display for screen reader announcements
    - `aria-valuetext` on time slider
    - Focus-visible styles for keyboard navigation
    - `role="group"` with `aria-label` on preset buttons container
  - Styling:
    - Uses Tailwind CSS custom properties (--color-accent, --color-panel, --color-input-bg, etc.)
    - Touch-friendly sizing for mobile (44px minimum touch targets)
    - Reduced motion preference respected
    - Consistent with existing design system
  - Updated `src/pages/index.astro`:
    - Imported TimeControls component
    - Replaced inline date/time controls with `<TimeControls />` component
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-004: Extract Visualization Toggles section
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Created VisualizationOptions component grouping all visualization toggles:
  - Created `src/components/controls/VisualizationOptions.astro`:
    - Clouds toggle with animation mode selector (Static/Animated)
    - Atmosphere toggle (checked by default)
    - Reference lines toggle
    - Terminator line toggle
    - Moon toggle
    - Aurora toggle
    - Sun intensity toggle
    - Satellite orbits toggle
    - Timezone boundaries toggle
  - Component structure:
    - Section header "Visualization" for visual hierarchy
    - Option groups with subtle separator borders
    - Cloud mode selector indented under clouds toggle
    - Consistent toggle styling across all options
  - All element IDs preserved for JS compatibility:
    - `cloudToggle`, `cloudModeSelect`, `atmosphereToggle`, `refLinesToggle`
    - `terminatorToggle`, `moonToggle`, `auroraToggle`, `solarIntensityToggle`
    - `satelliteToggle`, `timezoneToggle`
  - Styling:
    - Uses Tailwind CSS custom properties (--color-accent, --color-panel, etc.)
    - Touch-friendly sizing for mobile (44px minimum touch targets)
    - Focus-visible styles for accessibility
    - Reduced motion preference respected
    - Dark panel background for visual grouping
  - Updated `src/pages/index.astro`:
    - Imported VisualizationOptions component
    - Replaced 9 individual visualization toggle divs with `<VisualizationOptions />` component
    - Eclipse toggle kept separate (has additional UI: notification + quick-jump buttons)
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-005: Extract Markers section
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Created Markers component grouping city markers and custom markers:
  - Created `src/components/controls/Markers.astro`:
    - City markers toggle with checkbox and label
    - City search input with autocomplete dropdown
    - Custom markers subsection with add button and marker list
  - Component structure:
    - Section header "Markers" for visual hierarchy
    - Option groups with subtle separator borders
    - Search input with clear button positioned inside input
    - Custom markers header with title and count indicator
    - Add marker button with dashed border styling
    - Scrollable marker list container (max 150px height)
  - All element IDs preserved for JS compatibility:
    - `markerToggle`, `citySearch`, `citySearchClear`, `citySearchResults`
    - `customMarkerCount`, `addMarkerBtn`, `customMarkersList`
  - Styling:
    - Uses Tailwind CSS custom properties (--color-accent, --color-panel, etc.)
    - Touch-friendly sizing for mobile (44px minimum touch targets)
    - Focus-visible styles for accessibility
    - Reduced motion preference respected
    - Dark panel background for visual grouping
    - Distinct styling between toggle, search input, and add button
  - Updated `src/pages/index.astro`:
    - Imported Markers component
    - Replaced inline city markers toggle, city search, and custom markers section with `<Markers />` component
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-006: Extract Eclipse Controls section
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Created dedicated Eclipse Controls component with toggle and quick-jump buttons:
  - Created `src/components/controls/EclipseControls.astro`:
    - Eclipse toggle checkbox with label
    - Eclipse notification area for displaying eclipse info
    - Quick-jump buttons container for upcoming eclipses
  - Component structure:
    - Section header "Eclipses" for visual hierarchy
    - Toggle using consistent styling pattern
    - Notification styled with orange theme matching eclipse color
    - Buttons container with flex-wrap for responsive layout
  - All element IDs preserved for JS compatibility:
    - `eclipseToggle`, `eclipseNotification`, `eclipseButtons`
  - Styling:
    - Uses Tailwind CSS custom properties (--color-accent, --color-panel, --color-eclipse, etc.)
    - Touch-friendly sizing for mobile (44px minimum touch targets)
    - Focus-visible styles for accessibility
    - Reduced motion preference respected
    - Dark panel background for visual grouping
    - Eclipse notification with orange theme (rgba(255, 160, 0, *))
  - Updated `src/pages/index.astro`:
    - Imported EclipseControls component
    - Replaced inline eclipse toggle, notification, and buttons with `<EclipseControls />` component
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-007: Extract Overlay components
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Extracted loading and error overlays into separate Astro components:
  - Created `src/components/overlays/LoadingOverlay.astro`:
    - Full-screen loading overlay with spinner animation
    - Progress bar with percentage display
    - Status text showing loading progress
    - Uses Tailwind CSS utility classes
    - Supports reduced motion preference
    - Fade-out animation on completion
  - Created `src/components/overlays/ErrorOverlay.astro`:
    - Full-screen error overlay with warning icon
    - Error message display area
    - Retry button with hover/active states
    - Uses Tailwind CSS utility classes
    - Mobile touch-friendly sizing (44px minimum)
    - Focus-visible styles for accessibility
  - All element IDs preserved for JS compatibility:
    - `loading-overlay`, `loading-progress`, `loading-status`
    - `error-overlay`, `error-message`, `retry-button`
  - Updated `src/pages/index.astro`:
    - Imported LoadingOverlay and ErrorOverlay components
    - Replaced inline overlay HTML with component tags
  - Updated `src/styles/global.css`:
    - Removed legacy overlay CSS (now in components)
    - Added comments noting style locations
  - Accessibility features:
    - `role="alert"` and `aria-live` on overlays
    - `role="progressbar"` with aria-valuenow/min/max
    - Focus-visible styles on retry button
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-008: Extract Info Panels
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Extracted sun times display and instructions panel into separate Astro components:
  - Created `src/components/panels/SunTimesDisplay.astro`:
    - Sunrise/sunset info display for selected location
    - Header label with "Sunrise & Sunset" title
    - Dynamic content area updated by JS
    - Uses Tailwind CSS custom properties for theming
    - Global styles for JS-created elements (sun-rise, sun-set, etc.)
    - Mobile touch-friendly sizing
  - Created `src/components/panels/InstructionsPanel.astro`:
    - Keyboard/mouse control instructions list
    - Renderer info display area
    - Fixed positioning at bottom-right
    - Hidden on mobile touch devices to save screen space
    - Responsive adjustments for smaller screens
    - Uses Tailwind CSS custom properties for theming
  - All element IDs preserved for JS compatibility:
    - `sunTimesDisplay`
    - `rendererInfo`
  - Updated `src/pages/index.astro`:
    - Imported SunTimesDisplay and InstructionsPanel components
    - Replaced inline sun times and instructions HTML with component tags
  - Updated `src/styles/global.css`:
    - Removed legacy `.instructions`, `.renderer-info`, and `.sun-times-*` CSS rules
    - Added comments noting style locations in components
    - Updated mobile media query comment
  - Consistent styling with existing design system
  - All 263 tests pass
  - Build completes without errors

### UI-REFACTOR-009: Migrate global.css to Tailwind
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Migrated legacy global.css to use Tailwind utility classes via @apply directive:
  - Converted all remaining CSS rules to use Tailwind utilities where possible
  - Used `@apply` directive for cleaner utility composition
  - Preserved all custom CSS properties in `@theme` block (colors, backgrounds, etc.)
  - Organized styles into clearly documented sections:
    - Base Styles (body, canvas)
    - Menu Toggle Button (hamburger/X animation)
    - Controls Panel (positioning, transitions)
    - City Search (JS-dynamically-created items)
    - Custom Markers (JS-dynamically-created elements)
    - Eclipse Buttons (JS-dynamically-created)
    - Timezone Tooltip (JS-dynamically-created)
    - Accessibility: Focus Indicators
    - Reduced Motion Preference
    - Mobile Touch Targets & Responsive Layout
  - Removed legacy CSS rules that were duplicated in component `<style>` blocks
  - Kept only styles that are:
    1. Global base styles (body, canvas)
    2. Styles for JS-dynamically-created elements (can't be component-scoped)
    3. Menu toggle and controls panel (still in index.astro, not componentized)
  - File size reduced from 695 lines to 298 lines (57% reduction)
  - All component styles now live in their respective Astro `<style>` blocks
  - Visual appearance matches original design exactly
  - All 263 tests pass
  - TypeScript compiles without errors
  - Build completes without errors

### TOOLING-001: Fix JSX IntrinsicElements TypeScript error in IDE
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Fixed JSX IntrinsicElements TypeScript error that appeared in IDE for all Astro components:
  - Created `src/env.d.ts` with Astro type references:
    - `/// <reference path="../.astro/types.d.ts" />` for generated Astro types
    - `/// <reference types="astro/client" />` for Astro client types (IntrinsicElements, ImportMeta, etc.)
    - This is the canonical Astro approach for ensuring IDE type resolution
  - Fixed `src/components/ui/Panel.astro` type error:
    - Changed `role` prop type from `string` to `astroHTML.JSX.HTMLAttributes['role']`
    - This matches the `AriaRole` union type expected by Astro's JSX type system
    - Resolved `astro check` error: "Type 'string' is not assignable to type 'AriaRole | null | undefined'"
  - Verification:
    - `pnpm typecheck` passes (both `astro check` and `tsc --noEmit`)
    - `astro check` reports 0 errors, 0 warnings, 0 hints across 62 files
    - `tsc --noEmit` reports no errors
    - IDE now properly resolves HTML element types in .astro files
    - IntelliSense works for HTML attributes in Astro components
  - All 263 tests pass
  - Build completes without errors

### BUG-008: Control section needs to be scrollable
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Added scroll support to the controls panel for all viewport sizes:
  - Updated `.controls` in `src/styles/global.css`:
    - Added `max-height: calc(100vh - 40px)` to constrain panel to viewport height (20px margin top/bottom)
    - Added `overflow-y: auto` for vertical scrolling when content overflows
    - Added `-webkit-overflow-scrolling: touch` for smooth scrolling on iOS
    - Added `scrollbar-width: thin` and `scrollbar-color` for Firefox thin scrollbar styling
    - Added WebKit scrollbar pseudo-element styles (6px width, transparent track, subtle white thumb)
    - Scrollbar thumb has hover state for better visibility during interaction
  - Desktop behavior: panel scrolls with thin, themed scrollbar when content exceeds viewport
  - Mobile behavior: existing `max-h-[70vh]` and `overflow-y-auto` rules at 768px breakpoint still apply
  - Scrollbar styling matches dark UI theme (semi-transparent white on transparent background)
  - All controls accessible regardless of viewport size
- All 263 tests pass
- TypeScript compiles without errors
- Build completes without errors

### UI-REFACTOR-010: Replace component inline styling with Tailwind classes
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Replaced all inline `style="display: none;"` attributes in Astro components with Tailwind `hidden` class:
  - Updated `src/components/controls/EclipseControls.astro`:
    - Eclipse notification `div#eclipseNotification`: replaced `style="display: none;"` with `hidden` class
  - Updated `src/components/controls/TimeControls.astro`:
    - Pause icon `span#pauseIcon`: replaced `style="display: none;"` with `hidden` class
  - Updated `src/components/ui/SearchInput.astro`:
    - Clear button: replaced `style="display: none;"` with `hidden` class
  - Updated `src/components/controls/Markers.astro`:
    - City search clear button `button#citySearchClear`: replaced `style="display: none;"` with `hidden` class
  - All four elements were initially hidden with `style="display: none;"` and toggled by JavaScript via `element.style.display` assignments
  - Tailwind's `hidden` class outputs `display: none` (no `!important`), so JS inline style assignments continue to override correctly
  - No dynamic styles (`style={}` or `:style=`) found in any Astro components
  - Verified no inline `style=` attributes remain in any `.astro` file
- All 263 tests pass
- TypeScript compiles without errors (0 errors, 0 warnings via `astro check` and `tsc --noEmit`)
- Build completes without errors

### TOOLING-002: Add Biome configuration for linting and formatting
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Added Biome as the project's linter and formatter with project-appropriate configuration:
  - Installed `@biomejs/biome` v2.3.14 as devDependency
  - Created `biome.json` configuration file with:
    - Schema v2.3.14 matching installed CLI version
    - VCS integration (git) with `.gitignore` support
    - File exclusions: dist, docs, .astro, public/basis, public/service-worker.js
    - Formatter: 2-space indent, 100 char line width
    - JavaScript: single quotes, semicolons always, trailing commas
    - CSS: Tailwind directives enabled for `@apply`, `@import "tailwindcss"`, `@theme`
    - Linter: recommended rules with project-specific overrides:
      - `noForEach`: off (forEach used throughout codebase)
      - `noNonNullAssertion`: off (used with DOM elements)
      - `noParameterAssign`: off (common pattern in codebase)
      - `noExplicitAny`: off (matches existing TypeScript config)
      - `noAssignInExpressions`: off (used in some patterns)
      - `noUnusedVariables`: warn, `noUnusedImports`: warn
    - Override for `.astro` files: disabled `noUnusedVariables` and `noUnusedImports` (Biome can't analyze Astro frontmatter prop destructuring)
    - Assist: import organization enabled
  - Added npm scripts to package.json:
    - `pnpm lint`: runs `biome check .` (lint + format check)
    - `pnpm lint:fix`: runs `biome check --write .` (auto-fix safe issues)
    - `pnpm format`: runs `biome format --write .` (format only)
  - Auto-fixed codebase issues:
    - Formatted all 71 checked files to consistent style (50 files reformatted)
    - Converted `@ts-ignore` to `@ts-expect-error` (then removed unnecessary directive)
    - Applied optional chaining where applicable (`useOptionalChain`)
    - Replaced string concatenation with template literals (`useTemplate`)
    - Replaced `Math.pow()` with `**` operator (`useExponentiationOperator`)
    - Replaced `isNaN()` with `Number.isNaN()` (`noGlobalIsNan`)
    - Organized imports alphabetically across all files
  - Codebase passes Biome linting with zero errors and zero warnings
  - All 263 tests pass
  - TypeScript compiles without errors (0 errors, 0 warnings via `astro check` and `tsc --noEmit`)

### REFACTOR-003: Evaluate and remove unnecessary UI barrel export
- **Status:** COMPLETED
- **Date:** 2026-02-06
- **Changes:** Evaluated and removed the unnecessary UI barrel export file:
  - Analyzed `src/components/ui/index.ts`: file exported nothing (`export {}`) and only contained JSDoc comments
  - Searched codebase for any imports from `components/ui` barrel — found zero usages
  - All Astro UI components (Toggle, Button, Panel, SearchInput) are imported directly via their `.astro` paths
  - Barrel file provided no tree-shaking benefits (empty export) and no import convenience (Astro components can't be re-exported from TS)
  - Removed `src/components/ui/index.ts` as dead code
  - Import paths remain consistent (all direct imports, no barrel usage)
- All 263 tests pass
- Build completes without errors
- Biome linting passes with zero errors