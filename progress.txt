# 3D Earth Visualization - Implementation Progress

## Completed Tasks

### BUG-001: Remove duplicate getDayOfYear function
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Commit:** 3887624
- **Changes:** Removed duplicate `getDayOfYear` function from index.html. The function was defined twice (originally at lines 190 and 445) with identical implementation. Kept the first definition and removed the duplicate.

### BUG-002: Add Subresource Integrity (SRI) for CDN scripts
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added SRI `integrity` and `crossorigin="anonymous"` attributes to both CDN script tags:
  - Three.js r128: `sha384-CI3ELBVUz9XQO+97x6nwMDPosPR5XvsxW2ua7N1Xeygeh1IxtgqtCkGfQY9WWdHu`
  - OrbitControls: `sha384-wagZhIFgY4hD+7awjQjR4e2E294y6J2HSnd8eTNc15ZubTeQeVRZwhQJ+W6hnBsf`

### BUG-003: Fix potential XSS vulnerability in showError function
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Replaced unsafe `innerHTML` usage with safe DOM manipulation:
  - Refactored `showError()` function to use `document.createElement()` and `textContent` instead of template literals with `innerHTML`
  - Updated `initApp()` error handler to use safe DOM methods instead of `innerHTML +=`
  - Updated error messages to use plain text (removed `<br>` tags that relied on HTML parsing)
  - Malicious input is now escaped automatically since `textContent` treats all input as text

### BUG-004: Remove unused texture uniforms from shader
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Removed unused `normalTexture` and `specularTexture` that were loaded but never used in the fragment shader:
  - Removed texture loading for `textures/earth_normal.jpg` and `textures/earth_specular.jpg`
  - Removed `normalTexture` and `specularTexture` uniforms from ShaderMaterial
  - Removed unused uniform declarations from fragment shader
  - This reduces bandwidth usage and eliminates unnecessary texture downloads

### BUG-005: Cache DOM element references for performance
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Cached the 'currentTime' DOM element reference to avoid repeated getElementById calls:
  - Added `timeDisplayElement` global variable to store the cached reference
  - Modified `initTimeControls()` to cache the element reference during initialization
  - Updated `updateTimeDisplay()` to use the cached reference instead of calling `document.getElementById('currentTime')` on every update
  - This eliminates unnecessary DOM queries during slider movement for improved performance

### REFACTOR-001: Migrate to Astro + TypeScript project structure
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Converted single-file application to modular Astro project with TypeScript:
  - Created package.json with pnpm, Astro 5.16, Three.js 0.128, TypeScript 5.9, and @astrojs/cloudflare adapter
  - Created tsconfig.json with strict mode enabled and all strict checks
  - Created astro.config.mjs with Cloudflare adapter for static output
  - Created wrangler.toml with [assets] directory = "./dist" (per project guidelines)
  - Migrated HTML to src/pages/index.astro
  - Extracted CSS to src/styles/global.css
  - Moved textures to public/textures/
  - Created modular TypeScript architecture in src/scripts/earth/:
    - types.ts: All TypeScript interfaces (EarthConfig, SceneObjects, EarthObjects, etc.)
    - config.ts: Configuration constants with typed interface
    - scene.ts: Scene initialization, WebGL support checking, error display
    - earth.ts: Earth mesh, shader materials, cloud layer
    - lighting.ts: Ambient and directional lighting
    - astronomy.ts: NOAA sun position calculations, day of year
    - controls.ts: OrbitControls initialization
    - geolocation.ts: Browser geolocation handling
    - ui.ts: Time controls, event listeners, display updates
    - index.ts: Module re-exports
  - Created src/scripts/main.ts as orchestrating entry point
  - Build completes without TypeScript errors
  - No 'any' types used
  - All imports use relative paths (../scripts/)
  - Removed old index.html

### FEATURE-004: Add interactive location markers for major cities
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented interactive city markers with tooltips showing local time:
  - Created new `src/scripts/earth/markers.ts` module with full TypeScript types
  - Added 12 major world cities: London, New York, Tokyo, Sydney, Dubai, São Paulo, Mumbai, Beijing, Paris, Los Angeles, Moscow, Cairo
  - Each city has coordinates (lat/lon) and timezone offset for accurate local time calculation
  - Markers use THREE.Sprite with canvas-generated textures for billboard rendering (always face camera)
  - City markers are yellow (#ffcc00) filled circles with white borders
  - User location marker is green (#00ff88) with ring design for distinction
  - Implemented `latLonToPosition()` function for spherical coordinate conversion
  - Tooltips show city name and calculated local time based on time slider position
  - Local time calculation accounts for user's local timezone offset
  - Added UI toggle checkbox "Show city markers" to show/hide all markers
  - Markers rotate with Earth mesh to stay at correct geographic positions
  - Added `CityData` and `MarkerState` interfaces to types.ts
  - Updated index.ts exports and main.ts integration
  - All TypeScript strict mode checks pass
  - Build completes without errors

### FEATURE-002: Implement Earth's axial tilt for accurate seasonal visualization
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added Earth's 23.4 degree axial tilt for realistic seasonal day/night patterns:
  - Added `AXIAL_TILT: 23.4` constant to config.ts
  - Added `AXIAL_TILT` property to EarthConfig interface in types.ts
  - Modified `initEarth()` in earth.ts to apply the tilt to both Earth mesh and clouds layer via `rotation.z`
  - Updated `updateEarthRotation()` to preserve the tilt when adjusting longitude-based rotation
  - The tilt affects how the day/night terminator appears at different times of year
  - Arctic region now correctly shows near-24h daylight on June 21 (summer solstice)
  - Arctic region now correctly shows near-24h darkness on December 21 (winter solstice)
  - Equinoxes (March 20, September 22) show terminator passing through both poles
  - Build completes without TypeScript errors

### FEATURE-003: Add date picker for seasonal exploration
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Added date picker UI and connected it to sun position calculations:
  - Date picker UI element already existed in index.astro
  - TimeState interface already included `datePicker`, `lastDateValue`, and `selectedDate` fields
  - Helper functions `formatDateForInput()` and `parseDateFromInput()` already in ui.ts
  - Date picker initialization with current date as default already implemented
  - Event listener for date changes already added, updating `selectedDate` in TimeState
  - **Fixed bug:** `handleSunUpdate()` now passes `timeState.selectedDate` to `updateSunPosition()`
  - **Fixed bug:** `handleSunUpdate()` now detects date changes via `lastDateValue` comparison
  - Sun position now correctly updates when date is changed
  - Solar declination changes based on selected date, affecting day/night patterns
  - Time slider continues to work correctly in combination with date picker
  - Selecting June 21 shows longer daylight in Northern Hemisphere
  - Selecting December 21 shows shorter daylight in Northern Hemisphere
  - Build completes without TypeScript errors

### FEATURE-001: Add stars background for immersive space environment
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented a star field using THREE.Points for efficient rendering:
  - Created new `src/scripts/earth/stars.ts` module with full TypeScript types
  - Added `StarsConfig` interface for configurable star parameters
  - Stars use 5000 points distributed uniformly on a sphere with radius 400 (well behind Earth)
  - Used uniform spherical distribution algorithm for even star coverage in all directions
  - Stars rendered with THREE.PointsMaterial (white, size 1.5, slight transparency)
  - Stars positioned at correct z-depth (always behind Earth and clouds due to large radius)
  - Added to scene immediately after scene initialization but before Earth
  - Updated `src/scripts/earth/index.ts` to export stars module
  - Updated `src/scripts/main.ts` to initialize stars after scene setup
  - Build completes without TypeScript errors
  - No performance impact (particle systems are highly efficient)

### FEATURE-005: Add toggle for Earth axis and reference lines visualization
- **Status:** COMPLETED
- **Date:** 2026-01-24
- **Changes:** Implemented reference lines showing Earth's axis and latitude circles:
  - Created new `src/scripts/earth/reflines.ts` module with full TypeScript types
  - Added `ReferenceLinesState` interface to types.ts
  - Axis line (blue, 0x00aaff) extends through North and South poles beyond Earth surface
  - Equator line (orange, 0xffaa00) rendered as circle at 0 degrees latitude
  - Arctic Circle (green, 0x88ff88) rendered at 66.5 degrees N latitude
  - Antarctic Circle (green, 0x88ff88) rendered at 66.5 degrees S latitude
  - All lines use semi-transparent material (opacity 0.4) to avoid obscuring Earth texture
  - Reference lines grouped in THREE.Group with Earth's axial tilt applied
  - Lines rotate with Earth mesh to maintain correct geographic positions
  - Added UI toggle checkbox "Show reference lines" - disabled by default
  - Updated index.ts exports and main.ts integration
  - All TypeScript strict mode checks pass

### IMPROVEMENT-001: Add loading indicator for texture downloads
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented a comprehensive loading system with progress tracking and error recovery:
  - Created loading overlay UI in index.astro with spinner animation and progress bar
  - Added CSS styles for loading indicator with fade-out animation and error state
  - Created new `src/scripts/earth/loading.ts` module with full TypeScript types:
    - `LoadingState` interface for managing loading UI state
    - `createLoadingState()` to initialize loading state from DOM elements
    - `createTextureLoadingManager()` to create THREE.LoadingManager with progress tracking
    - `updateLoadingProgress()` for real-time progress updates
    - `hideLoadingOverlay()` with smooth fade-out animation
    - `showErrorOverlay()` and `hideErrorOverlay()` for error handling
    - `setupRetryButton()` for error recovery functionality
  - Modified `initEarth()` in earth.ts to accept LoadingState and use the loading manager
  - Updated main.ts to initialize loading state and wire up retry functionality
  - Loading overlay shows percentage progress during texture downloads
  - Error overlay displays when texture loading fails with retry button
  - Canvas remains hidden until all textures are loaded (no flash of incomplete content)
  - Build completes without TypeScript errors

### IMPROVEMENT-002: Add unit tests with Vitest
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented comprehensive unit test suite using Vitest:
  - Added `vitest` and `@vitest/coverage-v8` to devDependencies
  - Created `vitest.config.ts` with test configuration and coverage thresholds
  - Added test scripts: `pnpm test`, `pnpm test:watch`, `pnpm test:coverage`
  - Created test files:
    - `src/scripts/earth/astronomy.test.ts`: 17 tests for getDayOfYear() and calculateSunPosition()
      - Edge cases: Jan 1, Dec 31, leap year handling
      - Sun position calculations with known astronomical data
      - Summer/winter solstice declination verification
      - Equinox declination near-zero verification
    - `src/scripts/earth/markers.test.ts`: 23 tests for latLonToPosition() and calculateLocalTime()
      - Coordinate conversion for poles, equator, cities
      - Position magnitude verification
      - Local time calculation with various timezone offsets
      - Day rollover handling, fractional timezones (India UTC+5:30)
      - CITIES constant validation
    - `src/scripts/earth/config.test.ts`: 20 tests for CONFIG constant validation
      - Physical properties, time constants, camera settings
      - Lighting, visual effects, default location, controls
  - All 60 tests pass
  - Tests run in under 500ms
  - Build continues to work without errors

### FEATURE-011: Add time scrubber for full day visualization
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Enhanced time controls with play/pause functionality and speed controls:
  - Updated `TimeState` interface in types.ts with playback properties:
    - `isPlaying`: boolean to track playback state
    - `playbackSpeed`: number (1, 10, 60, 360 multipliers)
    - `lastPlaybackTime`: timestamp for calculating elapsed time
  - Added playback UI in index.astro:
    - Play/pause button with ▶ and ⏸ icons
    - Speed selector dropdown with 1×, 10×, 60× (default), and 360× options
  - Implemented playback logic in ui.ts:
    - `togglePlayback()`: toggle play/pause state and update UI
    - `setPlaybackSpeed()`: change playback speed multiplier
    - `stepTime()`: step forward/backward by slider step amount
    - `updatePlayback()`: calculate and apply time progression during playback
    - `updatePlaybackUI()`: sync play/pause icons with state
  - Added keyboard shortcuts:
    - Space: Play/Pause
    - Left Arrow: Step time backward
    - Right Arrow: Step time forward
  - Updated main.ts animate loop to call `updatePlayback()` each frame
  - Added CSS styles for playback controls:
    - Circular play/pause button with hover/active states
    - Speed selector styled to match other controls
  - Time wraps around at midnight (1440 minutes)
  - All 60 existing tests pass
  - Build completes without TypeScript errors

### FEATURE-012: Add fly-to animation for city markers
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented smooth camera fly-to animation when clicking city markers:
  - Created new `src/scripts/earth/flyTo.ts` module with full TypeScript types:
    - `FlyToState` interface for managing animation state
    - `createFlyToState()` to initialize animation state
    - `startFlyTo()` to begin camera animation to a city
    - `updateFlyTo()` to update animation each frame
    - `cancelFlyTo()` to interrupt animation on user interaction
    - `isFlyingTo()` helper to check animation status
  - Camera follows curved path using spherical interpolation (not straight line)
  - Animation duration 2-3 seconds based on angular distance
  - Ease-in-out cubic easing for smooth motion
  - Added click handler for city markers in main.ts
  - Added `handleMarkerClick()` function in markers.ts
  - User can interrupt animation via mouse drag or scroll wheel
  - Clicking another marker during animation starts new fly-to
  - Added tooltip hint "Click to fly to" on marker hover
  - Added pointer cursor style when hovering over markers
  - Controls disabled during animation, re-enabled on completion
  - Added 21 unit tests for fly-to functionality
  - All 81 tests pass
  - Build completes without TypeScript errors

### FEATURE-007: Add atmospheric glow effect at Earth's edge
- **Status:** COMPLETED
- **Date:** 2026-01-25
- **Changes:** Implemented atmospheric glow using Fresnel shader for realistic limb effect:
  - Created new `src/scripts/earth/atmosphere.ts` module with full TypeScript types:
    - `AtmosphereState` interface for managing atmosphere mesh and visibility
    - `initAtmosphere()` creates sphere with Fresnel shader material
    - `updateAtmosphereRotation()` syncs atmosphere with Earth rotation
    - `setAtmosphereVisible()` toggles visibility
  - Fresnel shader creates blue glow at Earth's edge (limb effect):
    - Vertex shader computes world position for view direction calculation
    - Fragment shader uses Fresnel equation: stronger glow at edges
    - Uses `THREE.BackSide` rendering for glow effect
    - Uses `THREE.AdditiveBlending` for light accumulation
  - Added atmosphere configuration constants to config.ts:
    - `ATMOSPHERE_RADIUS: 5.15` (slightly larger than clouds)
    - `ATMOSPHERE_COLOR: 0x88ccff` (sky blue)
    - `ATMOSPHERE_GLOW_INTENSITY: 0.6` (subtle glow)
    - `ATMOSPHERE_FRESNEL_POWER: 3.0` (controls edge sharpness)
  - Added `AtmosphereState` interface to types.ts
  - Added UI toggle checkbox "Show atmosphere" (enabled by default)
  - Atmosphere rotates with Earth mesh
  - Updated index.ts exports and main.ts integration
  - Added 4 unit tests for atmosphere config validation
  - All 85 tests pass
  - Build completes without TypeScript errors

### FEATURE-013: Expand city markers to 50+ locations with search
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Expanded city markers from 12 to 55 cities and added search functionality:
  - Expanded CITIES array to 55 cities spanning all continents:
    - Europe (14): London, Paris, Berlin, Madrid, Rome, Amsterdam, Stockholm, Athens, Istanbul, Moscow, Warsaw, Lisbon, Dublin, Helsinki
    - North America (9): New York, Los Angeles, Chicago, Toronto, Mexico City, Vancouver, Miami, San Francisco, Houston
    - South America (6): São Paulo, Buenos Aires, Rio de Janeiro, Lima, Bogotá, Santiago
    - Asia (15): Tokyo, Beijing, Shanghai, Mumbai, Delhi, Dubai, Singapore, Hong Kong, Seoul, Bangkok, Jakarta, Taipei, Riyadh, Tehran, Karachi
    - Africa (7): Cairo, Lagos, Nairobi, Johannesburg, Casablanca, Cape Town, Addis Ababa
    - Oceania (4): Sydney, Melbourne, Auckland, Perth
  - Added city search input with autocomplete dropdown in controls panel
  - Search is case-insensitive and supports partial matching
  - Search results debounced at 200ms for performance
  - Selecting a search result triggers fly-to animation to the city
  - Clear button to reset search input
  - Results limited to 8 matches in dropdown
  - Added `CitySearchState` interface to types.ts
  - Added `searchCities()`, `initCitySearch()`, `updateSearchResults()`, `clearCitySearch()` functions to markers.ts
  - Added CSS styles for search input, results dropdown, and clear button
  - Added 6 unit tests for `searchCities()` function
  - All 91 tests pass
  - TypeScript compiles without errors

### FEATURE-016: Add solstice and equinox preset buttons
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Added quick-access preset buttons for astronomically significant dates:
  - Added four preset buttons below the date picker: Spring Eq, Summer Sol, Autumn Eq, Winter Sol
  - Spring Equinox sets date to March 20 of current year
  - Summer Solstice sets date to June 21 of current year
  - Autumn Equinox sets date to September 22 of current year
  - Winter Solstice sets date to December 21 of current year
  - Clicking a preset sets the date and time to noon UTC for optimal visualization
  - Active preset button is visually highlighted with blue border and background
  - Active state clears when user manually changes the date picker
  - Each button has a tooltip explaining the astronomical significance
  - Added `getAstronomicalPresets()` exported function for testability
  - Buttons styled consistently with existing UI controls
  - All 91 existing tests pass
  - Build completes without TypeScript errors

### FEATURE-017: Highlight the day/night terminator line
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented visible terminator line at the day/night boundary with glow effect:
  - Created new `src/scripts/earth/terminator.ts` module with full TypeScript types:
    - `generateTerminatorPoints()` computes great circle points perpendicular to sun direction
    - `initTerminator()` creates the line geometry and glow line
    - `updateTerminatorPosition()` updates geometry each frame based on sun direction
    - `setTerminatorVisible()` toggles visibility
  - Terminator is the great circle where `dot(surfaceNormal, sunDirection) = 0`
  - Algorithm constructs orthonormal basis (U, V) in the plane perpendicular to sun direction
  - Points generated as `R * (cos(t) * U + sin(t) * V)` for 128 segments
  - Main line uses orange/gold color (0xffaa44) at 0.7 opacity
  - Glow line uses deeper orange (0xff8822) at 0.25 opacity behind the main line
  - Added `TerminatorState` interface to types.ts
  - Added UI toggle checkbox "Show terminator line" (disabled by default)
  - Terminator rotates with Earth mesh (same coordinate space as shader)
  - Earth's axial tilt applied to terminator group
  - Line updates in real-time as time/date changes
  - Updated index.ts exports and main.ts integration
  - All 91 tests pass
  - Build completes without TypeScript errors

### FEATURE-006: Add Moon visualization with correct orbital position and phases
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented Moon with orbital position calculation and phase lighting:
  - Created new `src/scripts/earth/moon.ts` module with full TypeScript types:
    - `calculateMoonPhaseAngle()` computes Moon phase using synodic period (29.53 days)
    - `calculateMoonPosition()` positions Moon relative to Sun-Earth line
    - `initMoon()` creates Moon mesh with custom shader for phase illumination
    - `updateMoonPosition()` updates position and lighting each frame
    - `setMoonVisible()` toggles visibility
  - Moon uses Lambertian lighting shader illuminated by sun direction for accurate phases:
    - New moon: Moon between Earth and Sun (dark face toward Earth)
    - Full moon: Earth between Moon and Sun (lit face toward Earth)
  - Orbital mechanics:
    - Phase angle calculated from reference new moon (Jan 6, 2000)
    - Orbital inclination of 5.14 degrees applied
    - Moon distance = 12x Earth radius (compressed from real 60x for visibility)
    - Moon radius = 0.2724x Earth radius (accurate ratio)
  - Tidal locking implemented: Moon always faces Earth (lookAt origin)
  - Added `MoonState` interface to types.ts
  - Added UI toggle checkbox "Show Moon" (disabled by default)
  - Moon position updates in real-time as time/date changes
  - Updated index.ts exports and main.ts integration
  - Added 10 unit tests for moon phase and position calculations
  - All 101 tests pass
  - TypeScript compiles without errors

### FEATURE-015: Display sunrise and sunset times for locations
- **Status:** COMPLETED
- **Date:** 2026-01-27
- **Changes:** Implemented sunrise/sunset calculation and display for all locations:
  - Created new `src/scripts/earth/sunrise.ts` module with full TypeScript types:
    - `SunTimes` interface for sunrise/sunset data with polar day/night flags
    - `calculateSunriseSunset()` uses NOAA algorithm with solar hour angle calculation
    - `formatSunTimesForTooltip()` formats sun times for marker tooltips
    - `updateSunTimesDisplay()` renders sunrise/sunset panel in UI
  - Algorithm details:
    - Uses NOAA equation of time and solar declination (same as astronomy.ts)
    - Calculates solar hour angle at sunrise/sunset using zenith angle 90.8333°
    - Accounts for atmospheric refraction and solar disk radius
    - Detects polar day (cosHourAngle < -1) and polar night (cosHourAngle > 1)
    - Converts UTC sunrise/sunset to local timezone
  - Updated marker tooltip in markers.ts to show sunrise/sunset times
    - `handleMarkerHover()` now accepts `selectedDate` parameter
    - Tooltip shows "Sun: ↑HH:MM ↓HH:MM" or polar day/night message
  - Added sunrise/sunset info panel in controls sidebar (index.astro)
    - Shows user location sunrise/sunset times
    - Shows selected city sunrise/sunset when clicking a city marker
    - Automatically updates when date changes
    - Styled with sunrise (gold) and sunset (orange) colors
  - Added CSS styles for sun times display in global.css
  - Updated main.ts integration:
    - Passes selectedDate to marker hover handler
    - Tracks selected city for sun times panel
    - Refreshes panel when date changes or city is clicked
  - Added 14 unit tests for sunrise calculations:
    - Normal mid-latitude locations
    - Sunrise before sunset verification
    - Arctic polar day (Tromsø, June 21)
    - Arctic polar night (Tromsø, December 21)
    - Antarctic polar day and night
    - Equator equinox accuracy (~6:00 sunrise, ~18:00 sunset)
    - Timezone offset handling (Tokyo +9)
    - Fractional timezone (India +5:30)
    - Summer vs winter day length comparison
    - HH:MM format validation
    - Tooltip formatting for all cases
  - All 115 tests pass
  - TypeScript compiles without errors

### FEATURE-009: Add satellite orbit visualization with ISS tracking
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented satellite orbit visualization with ISS, GPS, and Geostationary orbits:
  - Created new `src/scripts/earth/satellites.ts` module with full TypeScript types:
    - `SATELLITES` array with ISS (408km, 51.6° inclination, 92.68min period), GPS (20,200km, 55°), and Geostationary (35,786km, 0°) orbit data
    - `calculateSatellitePosition()` computes satellite position using simplified circular orbit model with RAAN precession
    - `generateOrbitPoints()` creates orbital track line geometry
    - `initSatellites()` creates orbit lines and marker sprites for all satellites
    - `updateSatellitePositions()` updates marker positions based on time and day of year
    - `updateSatellitesRotation()` keeps orbits synced with Earth rotation
    - `setSatellitesVisible()` toggles visibility
  - Orbit visualization:
    - ISS orbital path (red) at ~408km altitude with 51.6° inclination
    - GPS orbital path (green) at ~20,200km altitude with 55° inclination
    - Geostationary ring (blue) at ~35,786km in equatorial plane
    - Semi-transparent orbit track lines (opacity 0.35) for unobtrusive display
    - Satellite markers use canvas-generated sprites with glow effect
    - Marker scale varies by altitude for visibility
  - Orbital mechanics:
    - Position calculated from orbital period, inclination, and RAAN precession
    - ISS completes orbit every ~92.68 minutes
    - RAAN slowly precesses based on day of year
    - Geostationary orbit correctly stays in equatorial plane
  - Earth's axial tilt applied to satellite group
  - Added `SatelliteData` and `SatelliteState` interfaces to types.ts
  - Added UI toggle checkbox "Show satellite orbits" (disabled by default)
  - Updated index.ts exports and main.ts integration
  - Added 18 unit tests for satellite calculations:
    - SATELLITES constant validation
    - Orbital radius verification
    - Position periodicity (same position after one period)
    - Geostationary equatorial plane check
    - Orbit point generation and closed loop verification
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-003: Optimize mobile touch gesture handling
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented comprehensive mobile touch support for all interactions:
  - Updated `controls.ts`:
    - Disabled panning (not useful for globe) via `controls.enablePan = false`
    - Added `rotateSpeed: 0.8` for smoother touch rotation
    - Added `setupCanvasTouchHandling()` to prevent default browser touch behaviors on canvas
  - Updated `markers.ts`:
    - Added `detectMarkerAtPosition()` for coordinate-based marker detection (shared by mouse and touch)
    - Added `showMarkerTooltip()` and `hideMarkerTooltip()` utility functions
    - Refactored `handleMarkerHover()` to use shared detection/tooltip functions
  - Updated `main.ts`:
    - Added touch event listeners (touchstart, touchend, touchmove)
    - Implemented tap detection with distance threshold (15px) and timeout (300ms)
    - Touch tap on marker shows tooltip and triggers fly-to animation
    - Tooltip auto-hides after 3 seconds on touch devices
    - Multi-touch (pinch) cancels fly-to animation
    - Called `setupCanvasTouchHandling()` during initialization
  - Updated `index.astro`:
    - Viewport meta tag updated with `maximum-scale=1.0, user-scalable=no` to prevent double-tap zoom
    - Instructions updated to mention pinch zoom and tap-to-fly
  - Updated `global.css`:
    - Added `touch-action: none` on canvas for proper touch handling
    - Added `@media (pointer: coarse)` styles for minimum 44x44px touch targets
    - Ensured all interactive elements (buttons, inputs, checkboxes, search items) meet accessibility size requirements
    - Added responsive layout for controls panel on small screens (max-width: 480px)
    - Hidden instructions panel on touch devices to save screen space
    - Added `-webkit-overflow-scrolling: touch` for smooth scrolling in controls panel
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-004: Add accessibility features
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented comprehensive accessibility features for keyboard, screen reader, and reduced motion support:
  - Updated `src/pages/index.astro`:
    - Added `role="alert"` and `aria-live="polite"` to loading overlay
    - Added `role="progressbar"` with `aria-valuenow/min/max` to progress bar
    - Added `role="alert"` and `aria-live="assertive"` to error overlay
    - Added `role="region"` and `aria-label` to controls panel
    - Added `aria-label` to all checkboxes (clouds, markers, reference lines, terminator, moon, satellites, atmosphere)
    - Added `aria-label` to date picker, time slider, play/pause button, speed select
    - Added `aria-valuetext` to time slider for screen reader time announcements
    - Added `aria-hidden="true"` to decorative elements (spinner, error icon, play/pause icons)
    - Added `role="search"` and `aria-label` to city search container
    - Added `aria-autocomplete="list"` and `aria-controls` to search input
    - Added `role="listbox"` to search results container
    - Added `role="group"` and `aria-label` to preset buttons container
    - Added `aria-label` to each preset button
    - Added `aria-live="polite"` to time display and sun times display
    - Added `role="complementary"` to instructions panel
  - Updated `src/styles/global.css`:
    - Added `:focus-visible` styles with `2px solid #4da6ff` outline for all interactive elements
    - Added `@media (prefers-reduced-motion: reduce)` to disable loading spinner animation, fade transitions, and button transitions
    - Fixed color contrast: `.sun-times-loading` changed from `#666` to `#999` for WCAG AA compliance
  - Updated `src/scripts/earth/scene.ts`:
    - Added `role="img"` and `aria-label` to canvas element describing the 3D visualization
  - Updated `src/scripts/earth/ui.ts`:
    - Play/pause button `aria-label` updates dynamically based on playback state
    - Time slider `aria-valuetext` updates with human-readable time on every display update
  - Updated `src/scripts/earth/markers.ts`:
    - Search result items have `role="option"` attribute
    - Active search result items have `aria-selected="true"` attribute
  - All 149 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### IMPROVEMENT-005: Add JSDoc documentation for all modules
- **Status:** COMPLETED
- **Date:** 2026-01-30
- **Changes:** Added comprehensive JSDoc/TSDoc documentation to all 21 source modules and configured TypeDoc:
  - Added `@module` header documentation to all modules: types.ts, config.ts, scene.ts, earth.ts, lighting.ts, astronomy.ts, controls.ts, geolocation.ts, ui.ts, markers.ts, stars.ts, reflines.ts, loading.ts, flyTo.ts, atmosphere.ts, terminator.ts, moon.ts, sunrise.ts, satellites.ts, main.ts, index.ts
  - Added `@param` and `@returns` tags to all exported functions across all modules
  - Added property descriptions to all interfaces in types.ts (19 interfaces)
  - Added property descriptions to SunTimes, StarsConfig, and SatelliteData interfaces
  - Added `@example` code snippets for complex functions (latLonToPosition, getDayOfYear, CONFIG)
  - Added `@throws` tags where applicable (checkWebGLSupport, initWebGL)
  - Installed TypeDoc as dev dependency
  - Created `typedoc.json` configuration with entry points, output directory, and sorting options
  - Added `pnpm docs` and `pnpm docs:watch` scripts to package.json
  - TypeDoc successfully generates HTML documentation to `./docs` directory
  - 100% of exported functions, interfaces, and constants are documented
  - All 153 tests pass
  - TypeScript compiles without errors

### PERF-001: Implement Level-of-Detail (LOD) for Earth mesh
- **Status:** COMPLETED
- **Date:** 2026-01-28
- **Changes:** Implemented THREE.LOD for Earth and clouds with 3 detail levels:
  - Updated `src/scripts/earth/earth.ts`:
    - Replaced single `THREE.Mesh` with `THREE.LOD` for both Earth and clouds
    - Three LOD levels: High (128 segments, <15 units), Medium (64 segments, 15-30 units), Low (32 segments, >30 units)
    - Shared shader material across all LOD levels (no extra GPU memory for materials)
    - Added `updateEarthLOD()` function to update LOD each frame based on camera distance
    - Added `getLODLevels()` helper using config constants
  - Updated `src/scripts/earth/types.ts`:
    - Changed `EarthObjects.earth` from `THREE.Mesh` to `THREE.LOD`
    - Changed `EarthObjects.clouds` from `THREE.Mesh` to `THREE.LOD`
    - Added LOD config properties to `EarthConfig` interface
  - Updated `src/scripts/earth/config.ts`:
    - Added `LOD_HIGH_SEGMENTS: 128` for close-up detail
    - Added `LOD_HIGH_DISTANCE: 0`, `LOD_MEDIUM_DISTANCE: 15`, `LOD_LOW_DISTANCE: 30` thresholds
  - Updated `src/scripts/main.ts`:
    - Imported `updateEarthLOD` and called it each frame before render
  - Added 4 unit tests for LOD config validation
  - All 153 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

### FEATURE-008: Add Aurora Borealis and Australis visualization
- **Status:** COMPLETED
- **Date:** 2026-01-30
- **Changes:** Implemented animated aurora effects near polar regions with night-side rendering:
  - Created new `src/scripts/earth/aurora.ts` module with full TypeScript types:
    - `createAuroraBandGeometry()` generates ring geometry at specified latitude band
    - `initAurora()` creates northern and southern aurora meshes with animated shader
    - `updateAurora()` advances animation time and updates sun direction uniform
    - `updateAuroraRotation()` syncs aurora group with Earth rotation
    - `setAuroraVisible()` toggles visibility
  - Custom GLSL shader for aurora curtain effect:
    - Vertex shader passes UV coordinates, world position, and normals
    - Fragment shader creates layered sine wave curtain with pseudo-noise for organic feel
    - Color gradient transitions from green (base) through blue to purple (top)
    - Shimmer effect using high-frequency noise
    - Night-side culling: auroras only visible on dark side using sun direction dot product
    - Smooth twilight transition using smoothstep
  - Aurora Borealis positioned at ~67 degrees N geomagnetic latitude
  - Aurora Australis positioned at ~67 degrees S geomagnetic latitude
  - Band width of 8 degrees for realistic aurora oval extent
  - Uses THREE.AdditiveBlending for light accumulation (glow effect)
  - Additive blending with transparency (opacity 0.45) for unobtrusive display
  - Earth's axial tilt applied to aurora group
  - Added `AuroraState` interface to types.ts
  - Added aurora config constants to config.ts and EarthConfig interface:
    - `AURORA_LATITUDE: 67`, `AURORA_BAND_WIDTH: 8`
    - `AURORA_OPACITY: 0.45`, `AURORA_ANIMATION_SPEED: 0.8`
  - Added UI toggle checkbox "Show auroras" (disabled by default)
  - Updated index.ts exports and main.ts integration
  - Animation runs at 60fps using delta-time-based updates
  - Added 11 unit tests for aurora config and geometry:
    - Config validation (latitude range, band width, opacity, speed)
    - Geometry vertex count, index buffer, radius, latitude placement
    - UV coordinate range, normal unit length
  - All 164 tests pass
  - TypeScript compiles without errors
  - Build completes successfully

## Pending Tasks

### Phase 1 - Bug Fixes
- [x] BUG-001: Remove duplicate getDayOfYear function
- [x] BUG-002: Add Subresource Integrity (SRI) for CDN scripts
- [x] BUG-003: Fix potential XSS vulnerability in showError function
- [x] BUG-004: Remove unused texture uniforms from shader
- [x] BUG-005: Cache DOM element references for performance

### Phase 2 - Refactoring
- [x] REFACTOR-001: Migrate to Astro + TypeScript project structure
- [x] REFACTOR-002: Create modular code architecture (completed as part of REFACTOR-001)

### Phase 3 - Core Features
- [x] FEATURE-002: Implement Earth's axial tilt
- [x] FEATURE-003: Add date picker for seasonal exploration

### Phase 4 - Visual Enhancements
- [x] FEATURE-001: Add stars background
- [x] FEATURE-004: Add interactive location markers for major cities
- [x] FEATURE-005: Add toggle for Earth axis and reference lines

### Phase 5 - Critical Improvements
- [x] IMPROVEMENT-001: Add loading indicator for texture downloads
- [x] IMPROVEMENT-002: Add unit tests with Vitest

### Phase 6 - Interactivity Enhancements
- [x] FEATURE-011: Add time scrubber for full day visualization
- [x] FEATURE-012: Add fly-to animation for city markers
- [x] FEATURE-013: Expand city markers to 50+ locations with search
- [x] FEATURE-015: Display sunrise and sunset times for locations

### Phase 7 - Astronomical Features
- [x] FEATURE-006: Add Moon visualization with orbital position and phases
- [x] FEATURE-016: Add solstice and equinox preset buttons
- [x] FEATURE-017: Highlight the day/night terminator line

### Phase 9 - Advanced Features
- [x] FEATURE-009: Add satellite orbit visualization with ISS tracking

### Phase 10 - Accessibility & Documentation
- [x] IMPROVEMENT-003: Optimize mobile touch gesture handling
- [x] IMPROVEMENT-004: Add accessibility features

### Phase 10 - Accessibility & Documentation (continued)
- [x] IMPROVEMENT-005: Add JSDoc documentation for all modules

### Phase 8 - Visual Polish
- [x] FEATURE-008: Add Aurora Borealis and Australis visualization

### Phase 11 - Performance & Future-proofing
- [x] PERF-001: Implement Level-of-Detail (LOD) for Earth mesh
